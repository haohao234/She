# 编译预检清单 · 她的信息本 iOS 骨架

> 这份清单是为了让「第一次编译」这一步尽量短。
> 编写环境是 Windows，**没有 Xcode 与 iOS SDK** —— 而 Xcode 只跑在 macOS 上，
> 这是 Apple 的硬约束，不是能靠努力绕过的限制。
> 所以工程配好了两条取回报错的路（第 4 节），**其中一条不需要你有 Mac**。
> 能静态判断的问题**已经全部改掉了**（第 2 节，22 类）；
> 剩下只有三类问题**只有编译器能裁决**（第 3 节，每类给两种修法）。
> 你编译一次、把报错贴回来，我按行改。

---

## 0. 现状一页纸

| 项 | 内容 |
|---|---|
| 源文件 | **29 个 Swift**（主 App **24** + 通知扩展 3 + 分享扩展 2）+ 3 份 `Info.plist` + 3 个 entitlements |
| 工程声明 | `project.yml`（三个 target / 两份共享契约 / App Groups 全在这里）+ 1 条 CI 流水线 |
| 代码量 | Swift 共 **11760 行**（主 App 11037 + 通知扩展 363 + 分享扩展 360） |
| 静态检查 | **十份脚本全通过**（九份静态 + 一份会开真浏览器）：`check-swift.js` · `check-argorder.py` · `check-project.py` · `check-guide.py` · `check-symbols.py` · `check-handoff.js` · `check-prototype.js` · `check-spec.js` · `check-globalsel.js` · `check-imgrow-live.py`。**各项项数只在第 5 节写一份** —— 这一行原来抄了一份，而且抄的时候就已经是旧的（写 27 项时实际已经 39），2026-09-15 删掉 |
| 怎么编译 | GitHub Actions（不用 Mac）或 `xcodegen generate`（有 Mac）—— 见第 4 节 |
| 云端编译状态 | **次数不在这里写死，按 API 现算**（这一行原来写「43 次成功 / 6 次失败，连续 11 次成功」—— 写下那一刻它就不再成立了，2026-09-15 改掉）。已发生的失败只有三段：**run 1~3**（搭流水线时的早期红叉）与 **run 36~38**（`16aaec9` → `f38c463` → `5ff7161` 连红三次，都是同一处 `CycleView.swift:147:22: error: cannot find 'shortDay' in scope` —— `statRow` 调了一个根本没写的函数）；**第三段 = `62e02e4` / `a7b4ce6`**（`Models.swift:979:5: error: '@discardableResult' attribute cannot be applied to this declaration` —— **这次是本地九份校验全绿却推上去的**，见 §3.8）。**结论按 GitHub API 实算，不是自报**（`/actions/runs` 拉全量后统计）。**「0 warning」现在是断言不是声明**（见下面的说明） |
| 已知风险 | 只有第 3 节那三类 |

> **⚠️ 这一类「累计数字」必须带锚点。** 上面那个数是**截至 `bdd7a6b`（run #49）的快照**，
> 因为**记录它的这个提交自己也会跑一次 CI** —— 写成「当前」的那一刻它就已经不是当前了。
> 这正是「从第 4 轮起没有一次回退」翻车的同一个机制：**一句会被推翻的话比一个过时的数字更坏，
> 它让人不再去查。** 要现成的数，看 Actions 页面或 `GET /repos/haohao234/She/actions/runs`。

> **「0 error / 0 warning」这句话曾经是错的 —— 从第 12 轮一直错到第 25 轮，连续 14 轮。**
> 已经拉出第 12 轮的日志核过：同一个文件、同一行（`VersionHistoryView.swift:84:35`）、
> 同一条警告，那一轮自报的就是 `warning 总数：1`。
>
> `VersionHistoryView.swift`（07 屏那一轮新建的文件）里有一句
> `} else if let c = current {`，那个 `c` **从头到尾没被用过**，
> 编译器一直在报 `value 'c' was defined but never used`。
> **每一轮编译都在报，只是没人看见** —— 我们只看 Actions 那个绿勾，
> 没看流水线自己数出来的 `warning 总数`。
> **「绿」只回答「编译过了吗」，不回答「干净吗」。**
>
> 两处改动让它不能再悄悄发生：
> ① 那一句改成 `current != nil`（这个分支只判断「有没有一版」，本来就不需要那一版本身）；
> ② 流水线加了一条**会红的步骤** —— 有 warning 就失败。
> **从此「0 warning」是断言，不是声明。**
>
> 代价要讲清：**runner 的 Xcode 升级带来新的 deprecation 警告时，CI 也会红。**
> 那种红是提醒、不是回归 —— 看清单就知道是什么，改掉或明确接受都行，
> **唯独不能让它在日志里静静躺着。**

```
project.yml                      工程声明：三个 target / 两份共享契约 / App Groups
.github/workflows/ios-build.yml  云端编译 + 打 IPA + 拆包自检（不用 Mac 也能拿到报错与可侧载的包）

HerInfo/                          主 target（24 个 Swift，含两份契约）
├─ HerInfoApp.swift                    168   ← 入口 + 三个入口统一的落库 drainInbox
├─ Info.plist                           96   ← 四条「少了不报错」的键 + 长按菜单三项
├─ HerInfo.entitlements                 25   ← App Group
├─ Shared/NotificationContract.swift   249   ← 三个 target 在 project.yml 里都列了它
├─ Shared/ShareContract.swift          192   ← 主 App + 分享扩展（引用上面的 App Group）
├─ DesignSystem/Tokens.swift           411
├─ DesignSystem/Components.swift      1198
├─ Models/Models.swift                 936
├─ Features/HomeView.swift             868   ← 01 首页 / 04+28 搜索
├─ Features/BrowseView.swift           539
├─ Features/EditorView.swift           689   ← 03 新增 / 21 引导 / 34 配图 / 08 键盘工具栏
├─ Features/ReminderEditorView.swift   369
├─ Features/CycleView.swift           1019   ← 37~41 生理期（周期 / 日历 / 记录）
├─ Features/ProfileEditView.swift      241
├─ Features/ExportView.swift           309
├─ Features/OnboardingView.swift       280   ← 15 / 16 首次使用
├─ Features/StarterPrompts.swift       225   ← 06 起手卡 / 21 引导提示条
├─ Features/VersionHistoryView.swift   493   ← 07 左右对比
├─ Features/SearchFilterView.swift     325   ← 30 筛选面板 / 04 结果统计行
├─ Features/MoodView.swift             541   ← 09 打标 / 10 建议面板
├─ Features/MoodIntent.swift            96   ← 09 快捷指令（AppIntent 写在主 App 里）
├─ Features/SecondaryViews.swift       661
├─ Services/PhotoStore.swift           303   ← 相册 → 压缩副本 → 内容哈希；含占用统计（无入口，见下）
├─ Services/ReminderService.swift      361
├─ Services/CycleService.swift         199   ← 周期预测与统计（纯计算，不碰 UI）
└─ Services/ExportService.swift        365

HerInfoNotification/              扩展 target（独立进程，3 个 Swift）
├─ NotificationViewController.swift    155
├─ NotificationContentView.swift       149
├─ NotificationStyle.swift              59
├─ Info.plist                          85   ← 六个键，见交接文档
└─ HerInfoNotification.entitlements    15

HerInfoShare/                     分享扩展 target（独立进程，2 个 Swift）
├─ ShareViewController.swift           111   ← 取分享内容 → 丢收件箱 → 关闭（不写库）
├─ ShareComposeView.swift              249   ← 面板：原文预览 + 四个分类胶囊
├─ Info.plist                          90   ← 四个键（share-services / ActivationRule）
└─ HerInfoShare.entitlements           20   ← 与上面那个**同一个** App Group
```

> **`PhotoStore.totalBytes()` / `humanSize()` 有实现、没有入口。** 12 屏内容区已经排满，
> 加「占用空间」这一行得挤掉别的行，所以按约定不加。这两个函数留着，
> 是因为它们与 `referencedHashes()` 同属「本地图片的家底」—— 真要加那一行时
> 直接 `humanSize(totalBytes())` 就行，不必回头补实现。**没有入口 ≠ 漏写。**

---

## 1. 建工程时最容易漏的五件事 —— 现在漏不掉了

这五步漏任何一步，症状都不是编译报错，而是「跑起来不对」。
**它们现在全部由 `project.yml` 声明，不需要手工操作** ——
下表留着，是为了让你知道「如果哪天要脱离 project.yml 手工建工程，
哪五处是必须盯住的」。

| # | 动作 | 漏了会怎样 | 现在由谁保证 |
|---|---|---|---|
| 1 | 两份契约**各自进该进的 target 的 Compile Sources**（`NotificationContract.swift` 进 **3** 个、`ShareContract.swift` 进 **2** 个） | 编译期就会报「找不到 `HINotify` / `HIShare`」—— 这一条是**会报错**的，好办 | `project.yml` 里每个 target 的 `sources` 都列了它们 + `check-project.py` 核对引用数 |
| 2 | **三个 target 都加** App Groups → `group.com.herinfo.app`（**同一个组**，别各开一个） | 两个收件箱静默失效：锁屏点胶囊没反应、分享出去的内容 App 永远等不到 | 三份 `.entitlements` + `check-project.py` 守住四处同名 |
| 3 | 通知扩展 `Info.plist` 的 `UNNotificationExtensionCategory` = **`HI_REMIND`** | 通知照弹，**长按展开回到系统默认样子** —— 不报错、不崩溃，只是白做 | `Info.plist` 里写死 + `check-project.py` 与契约逐字比对 |
| 4 | 两个扩展 `Info.plist` 的 `CFBundlePackageType` = **`XPC!`** | 系统不把这个 bundle 认成扩展，**连扩展进程都不会起** | 同上 |
| 5 | 分享扩展 `Info.plist` 的 `NSExtensionPointIdentifier` = **`com.apple.share-services`**，且 `NSExtensionActivationRule` 是 `NSExtensionAttributes` 里的**那个 dict** | 前者 → 分享面板里**永远不出现**；后者 → 出现了但点什么都没反应。都不报错 | `check-project.py` 第 5 段 + `check-swift.js` |

**第 5 条是本工程真写错过的一条**：那两条 `NSExtensionActivationSupports*` 是
`NSExtensionActivationRule` 的**子键**，第一版直接挂在 `NSExtensionAttributes` 下 ——
`check-project.py` 当场把它报了出来（少了外层，那两条就成了没人读的孤儿）。

另外主 target 的 `Info.plist` 现在是真文件了（之前骨架里缺这一份），
四条「少了不报错」的键都在里面，每条都注了「缺了会怎样」。

---

## 2. 已经改掉的（静态就能判断的，22 类）

这些是通读 5524 行时抓出来的。前 6 类**本来编译不过**，后面的是「能跑但不对」。

### 2.1 编译阻断项（6 类）

| # | 位置 | 症状 | 改法 |
|---|---|---|---|
| 1 | `ExportService.swift` · 嵌套结构 | `let photos: [P]` —— 类型名对不上，嵌套结构体叫 `Pic` | 改为 `[Pic]`，顺手补上 `Rev` 与 `revisions` |
| 2 | `ReminderService.swift` · 地理围栏回调 | 同步 delegate 里直接调 `UNUserNotificationCenter.current().add(req)`，而 `add` 是 `async throws` | 包成 `Task { try? await ... }` |
| 3 | `EditorView.swift` · `Pill(...)` | 参数顺序错（`tiny` 写在 `tint` 前面）：`argument must precede` | 按属性声明顺序改成 `text / tint / soft / tiny`（⚠️ `tiny` 这个参数 **2026-09-14 已删**，换成 `size: PillSize`，见 3.4 节） |
| 4 | `SecondaryViews.swift` · `AppearanceView` | `ForEach(modes, id: \.0)` —— KeyPath 不能指向元组元素 | 新增 `private struct ThemeMode: Identifiable` |
| 5 | `Components.swift` · 三个组件 | `SegmentControl` / `SField` / `PhotoGrid` 有 `private` 存储属性，逐成员初始化器**整体降级为 private**，跨文件调用报 inaccessible（报错落在调用方，极难归因） | 三处各补显式 `init` |
| 6 | `BrowseView.swift` · `NavRow` | 调用处 `NavRow("排序与置顶", onBack:)` 语法不成立 | 按 `NavRow` 的真实签名补齐 |

### 2.2 逻辑缺陷（16 类：能跑，但不对）

| # | 位置 | 问题 | 改法 |
|---|---|---|---|
| 7 | 7 个文件共 16 处 | `.navigationBarHidden(true)` 已弃用 | 统一换成 `.toolbar(.hidden, for: .navigationBar)` |
| 8 | `HomeView` / `BrowseView` / `ReminderListView` | tab 根屏对**空** `path` 调 `removeLast()` → `fatalError`。触发时机是「先点 tab 再点返回」，看起来不像 bug | 根态不显示返回键（`backAction` 改为可选，`path.isEmpty ? nil : {...}`） |
| 9 | `BrowseView` · 分类页顶行 | 非根态才有返回键，根态会缺一块宽度 | 根态用等宽占位（30×44 透明块） |
| 10 | `BrowseView` · `RecordDetailView` | 删除记录后**没落盘**，重启记录回来 | 补 `try? ctx.save()` |
| 11 | `ProfileEditView` · `save()` | 三处错：Revision 插在 `ctx.save()` **之后**、`version` 写死 1、关联键含名字（`profile:<名字>` 改个称呼就断链） | Revision 移到 save 之前；`version: existing + 1`；关联键固定为 `"profile"` |
| 12 | `SecondaryViews` · 通知权限 | 通知被关后跳的是「应用锁」页 | 改为 `.notifyDenied`，并新建 `NotificationDeniedView`（24 屏） |
| 13 | `HomeView` · `ReminderListView` | `SoftSwitch(isOn: .constant(m.isOn))` —— 开关**拨不动** | 改真绑定，并在变化时 `schedule` / `cancel` + `save` |
| 14 | `ExportView` | 「历史版本」开关记下了选择，却没把版本交给导出 —— 假开关 | 加 `@Query private var revisions`，传给 `export(...)` |
| 15 | `ExportService` | `revisions` 为 nil 与空数组没区分 | 用 `[Rev]?`：`nil` = 这次没导，`[]` = 这条没有历史 |
| 16 | `ExportService` | PDF 里铺开了全部历史版本 | PDF **刻意不带历史版本**（PDF 是给别人看的） |
| 17 | `ReminderEditorView` | `已用 3 / 20` 写死 | 改读 `ReminderService.shared.usedGeofences` |
| 18 | `ReminderEditorView` | `#Predicate<Record> { $0.id == rid }` 漏了泛型参数 | 补 `<Record>` |
| 19 | `EditorView` | `save(bump:)` 无条件涨版本 —— 点一下「完成」就多一版 | 加 `lastSnapshot` + `snapshot` 指纹，**只在内容真变时**才涨 |
| 20 | `EditorView` | 编辑器里的提醒开关改了不落库、不排程 | 新增 `syncReminder(for:)`，把三种情况（新建 / 改时间 / 关掉）分清 |
| 21 | `ReminderService` | 重复 `startMonitoring` 同一区域：不报错，也不更新半径 | 已在监听同一 id 就跳过 |
| 22 | `ReminderService` | `cancel` 只删主通知，`snooze_\(id)_` 前缀的副本留下 | `cancel` 改 `async`，连带清掉前缀副本 |

### 2.3 跨进程与色值（这一类是「架构级」的，单独说）

| # | 内容 | 为什么这么做 |
|---|---|---|
| 23 | 新建 `Shared/NotificationContract.swift` | 通知扩展是**独立进程**，两边都要用的字符串写两遍就一定分叉。分类 id、动作 id、userInfo 的键、App Group 名、五个情绪档、12 个共用色值，全收在这一个文件里，且**只 import Foundation + UserNotifications**（不引 SwiftData / SwiftUI） |
| 24 | `Tokens.swift` 与 `ExportService.swift` 里的字面色值全部改为引用契约 | 同一个主色要在三处一模一样：屏幕（动态色）、锁屏通知（深色档）、导出的 PDF（浅色档，打印语义）。`NotificationStyle.swift` 里**一个 hex 都没有** |
| 25 | `Models.swift` · `MoodLevel` 转发到 `HINotify.Mood` | 五个情绪档的 `rawValue` 是扩展与主 App 之间的桥，改一个必须改另一个（`check-swift.js` 会校验） |
| 26 | `HerInfoApp.swift` · `onChange(of: scenePhase)` → `drainInbox()` | 打标落库的**唯一入口**。扩展只管往收件箱里丢，主 App 在启动/回前台时取走 |
| 27 | `Models.swift` · 新增 `drainMoodInbox` / `markReminderDone` | 前者把打标落成 `Mood(source: .lockscreen)`；后者只关 `isOn`，**不删记录** |
| 28 | `HomeView` · `RootView` | `onReceive(.openRecord)` → 跳到记录详情；`onReceive(.reminderDone)` → `markReminderDone` |

---

## 3. 只有编译器能裁决的三类

这三类我按最保守的写法处理了，但**宏与并发的最终行为只有编译器知道**。
每类给两种修法，你把报错贴回来我直接选一种改。

### 3.1 Swift 6 严格并发下的 `nonisolated`

**我在哪儿用了它：** `ReminderService` 里 `UNUserNotificationCenterDelegate` 与
`CLLocationManagerDelegate` 的两个回调。

**可能的报错（任一方向）：**

```
Main actor-isolated instance method 'userNotificationCenter(_:didReceive:)' cannot be used
to satisfy nonisolated protocol requirement

Call to main actor-isolated instance method '...' in a synchronous nonisolated context
```

**为什么会这样：** delegate 协议本身是 `nonisolated` 的（系统在任意线程回调），
而我们的类是 `@MainActor`。Swift 6 不允许「用主线程隔离的方法去满足非隔离的协议要求」，
必须显式声明「这个方法我知道自己不在主线程」，再把跨界数据缩到 Sendable 纯值。

**修法 A（当前写法）：** 方法签名前加 `nonisolated`，函数体内**只取纯值**再跨界：

```swift
nonisolated func userNotificationCenter(
    _ center: UNUserNotificationCenter,
    didReceive response: UNNotificationResponse
) async {
    // 只取 String / Int 这类 Sendable 值 —— 不要跨边界传 userInfo 字典
    let rid = response.notification.request.content.userInfo["hi.recordID"] as? String
    let action = response.actionIdentifier
    await MainActor.run { handle(action, rid) }
}
```

**修法 B（如果编译器不接受 async 版本）：** 换成同步的旧签名 + `Task`：

```swift
nonisolated func userNotificationCenter(
    _ center: UNUserNotificationCenter,
    didReceive response: UNNotificationResponse,
    withCompletionHandler completion: @escaping () -> Void
) {
    let rid = response.notification.request.content.userInfo["hi.recordID"] as? String
    Task { @MainActor in
        handle(rid)
        completion()
    }
}
```

**不推荐的第三条路：** 把 target 的 Swift Language Version 降到 5。
它会让这一片安静下来，但你同时失去了「哪些代码真的在主线程」的保证 ——
而这个 App 的数据层全靠这条保证。

### 3.2 `#Predicate` 宏的捕获限制

**我在哪儿用了它：** `ReminderEditorView`（按 `recordID` 找记录）、
以及几处按 `deletedAt` / `isOn` 过滤的查询。

**可能的报错：**

```
Cannot convert value of type 'PredicateExpressions.Equal<...>' to expected argument type 'Predicate<Record>'

Expression 'rid' is not supported in this context
```

**为什么会这样：** `#Predicate` 是把闭包**在编译期展开成表达式树**的宏。
它只认得有限几种写法，而且报错通常**不指向现场** —— 它会指到整个 `@Query` 或
`FetchDescriptor` 那一行，让你以为是自己传错了参数。

**修法 A（当前写法）：** 把外部变量先拷成本地简单标识符再进闭包：

```swift
let target = rid                      // 先落地成局部常量
let fd = FetchDescriptor<Record>(predicate: #Predicate<Record> { $0.id == target })
```

**修法 B（如果宏还是不认）：** 干脆不用谓词 —— 6 张表、单机数据量，
全量取回来在内存里 `first(where:)` 完全可接受，而且更好调试：

```swift
let all = try ctx.fetch(FetchDescriptor<Record>())
guard let rec = all.first(where: { $0.id == rid }) else { return }
```

**取舍：** 记录量到几千条以上时再回到谓词。**现在不值得为它调半天。**

### 3.3 自定义 `Layout`（`FlowLayout`）的边界情况

**我在哪儿用了它：** `Components.swift` 里给 `PhotoGrid` 用的流式布局。

**可能的报错：**

```
Type 'FlowLayout' does not conform to protocol 'Layout'
Value of type 'FlowLayout' has no member 'sizeThatFits(proposal:subviews:cache:)'
```

**为什么会这样：** `Layout` 协议有两个方法要 `proposal.width == nil`、
`spacing`、`cache` 等边界要处理。手写一个正确的 `Layout` 需要真机跑过几种极端宽度。

**修法 A：** 对着协议把签名补齐，返回值用 `ProposedViewSize`：

```swift
func sizeThatFits(proposal: ProposedViewSize, subviews: Subviews, cache: inout ()) -> CGSize
func placeSubviews(in bounds: CGRect, proposal: ProposedViewSize, subviews: Subviews, cache: inout ())
```

**修法 B（我更推荐）：** 删掉 `FlowLayout`。34 屏的图片网格是**固定一行 3 个**
（105×3 + 10×2 = 335 = 375 − 40，这是算出来的，不是凑的），
一个 `LazyVGrid(columns: .init(repeating: .init(.flexible()), count: 3))` 就够：

```swift
LazyVGrid(columns: Array(repeating: GridItem(.flexible(), spacing: 10), count: 3), spacing: 10) {
    ForEach(photos) { PhotoCell($0) }
}
```

**为什么推荐 B：** 少一个自维护类型，就少一处只有极端字号下才暴露的 bug。
设计里没有「一行几个看卡片宽度」的需求，**别为不存在的需求写布局引擎。**

---

### 3.4 已经跑过真实编译了 —— 上面三类一个都没出现

> 这一节是**事后补的**。上面预测了三类「只有编译器能裁决」的问题；
> 真正在云端 macOS 上跑完一轮之后，报出来的是**另外两处**，而且都在通知扩展里。

**怎么跑的：** 代码推上 GitHub → `.github/workflows/ios-build.yml` 在 `macos-15` 上
执行 `xcodegen generate` + `xcodebuild build`（不签名、不跑模拟器）。

| 轮次 | 结果 | 报出的错 |
| --- | --- | --- |
| 第 1 轮 | 失败（2 个错） | `NotificationViewController.swift` 两处 `cannot find type 'UNNotificationContentExtension' in scope` / `…ResponseOption…` |
| 第 2 轮 | 失败（1 个错） | 同一文件 121 行 `value of type 'NSExtensionContext' has no member 'dismissNotification'` |
| 第 3 轮 | 失败（1 个错） | 扩展已通过，报错进到主 App —— `BrowseView.swift:297 argument 'tint' must precede argument 'tiny'` |
| **第 4 轮** | **成功** | `** BUILD SUCCEEDED **` · error 0 · warning 0 · 产出 `HerInfo.app`（含内嵌扩展） |
| **第 5 轮** | **成功（回归）** | 补「配图」落盘层（新增 `PhotoStore` + 改 6 个文件）后再编一次 —— 依然 0 error / 0 warning |
| **第 6 轮** | **成功（回归）** | 文档同步（预检清单第 6 节 + 交接文档）后再编一次，全步骤绿 |
| **第 7 轮** | 见 Actions | 引导屏 + 轻提示 + 删除二次确认 + 回收站（`ToastCenter` 这个跨屏单例是唯一的结构性新东西） |

**第 5 轮的意义和第 1–4 轮不同：它一次错都没报。** 原因是这一轮改动的三类风险
（`private` 存储属性导致 init 降级、`PhotosUI` 没 import、参数顺序）**全在本地就被拦住了**
（见下面 3.5 节）。也就是说：**能静态判的东西不该花云端编译去发现** ——
云端那一轮应该只用来裁决「查文档也定不了的 SDK 细节」。

**「能编译」这一关到此结束。** 三轮修掉的三个错误，没有一个属于第 3.1 / 3.2 / 3.3 节
预测的那三类 —— 全是「库名 / 符号名 / 参数顺序」这类查文档就能定的事。

**主 App 那 5500 行是一次通过的，0 warning。** 第 3.1 / 3.2 / 3.3 节担心的
`#Predicate` 捕获、Swift 6 并发、自定义 `Layout`，编译器一条都没报。

但这**不等于那三类不存在**，只是被三件事按住了：

- `SWIFT_VERSION` 还停在 `5.0` + `SWIFT_STRICT_CONCURRENCY: minimal` →
  3.1 节那批并发问题现在只是「没被检查」，转成 `6.0` 就会升级为错误；
- `#Predicate` 写的是内联字面量 → 3.2 节的捕获限制刚好绕过去了；
- `FlowLayout` 走的是保守估计 → 3.3 节的边界情况要真机才撞得上。

**这两个错的共同点：都是「库名差一点」。**

1. `UNNotificationContentExtension` 和 `UNNotificationContentExtensionResponseOption`
   属于 **UserNotificationsUI**，不属于 `UserNotifications`。
   两个库名字太像，只 import 后者就会报 `cannot find type … in scope` ——
   第一眼很容易误判成「iOS 版本不对」或「target 配错了」。
   → 补 `import UserNotificationsUI`，并在 `project.yml` 里**显式声明**
   `UserNotificationsUI.framework` 依赖（不要靠 Swift 自动链接兜底）。
2. 收回展开的卡片是 `extensionContext?.dismissNotificationContentExtension()`。
   **它不叫 `dismissNotification()`** —— 后者根本不存在，报 `has no member`。
   而且它**只收回卡片、不撤通知**（WWDC 2018 场次 710 明确讲过这一点）；
   要真的把通知从通知中心拿掉，得另外调
   `UNUserNotificationCenter.current().removeDeliveredNotifications(withIdentifiers:)`。
   两个动作少任何一个，用户看到的就是「点了没反应」或「打标了通知还赖着」。

**结论（比预测有用）：** 编译期真正卡人的，多半不是架构问题，而是
「这个符号属于哪个 framework、它到底叫什么」这种查一眼文档就能定的事。
所以第 4 节的修复协议里，**先看错误行、再查符号属于哪个 framework**，
比先怀疑自己的架构要快得多。

**第 3 类错误（`argument X must precede argument Y`）现在本地就能扫掉。**
它的成因是：Swift 的 memberwise initializer **参数顺序 = 存储属性声明顺序**。
`Pill` 声明是 `text / style / tint / soft / tiny / onTap`，
有一处调用写成了 `tiny:…, tint:…` —— 于是报 `argument 'tint' must precede argument 'tiny'`。

> ⚠️ **`tiny` 已经不存在了**（2026-09-14 换成 `size: PillSize`）。现在的声明顺序是
> `text / style / tint / soft / size / emphasis / textTint / onTap`。
> 上面那句只当历史记录，**别照着写**。校验脚本是从源码推导顺序的，会自动跟上。

这一类纯静态可判，不该花一轮云端编译去发现。已加扫描脚本
**`.workbuddy/checks/check-argorder.py`**：解析出每个类型的存储属性顺序，
再遍历所有调用点的顶层参数标签，检查是否为声明顺序的子序列。
（自测方式：故意造一个 `Pill(text:…, size:…, tint:…)` 的调用，必须能报出来 ——
**校验脚本自己也要有自测，否则很容易写成恒真的空转。**）
另：`check-swift.js` 还有一条守卫直接拦住残留的 `tiny:`（它现在是编译错误，不是被忽略），
**多一道网，就是为了别再花一轮 3-5 分钟的云端编译。**

> 顺带一条经验：`HerInfo/Features/EditorView.swift` 里本来就有一行注释写着
> 「把 tiny 写在 tint 前面是编译不过的」—— 说明这个坑**踩过一次、写了注释，
> 但另一个调用点还是漏了**。**注释挡不住重复犯错，扫描脚本才能。**

### 3.5 补「配图」那条线时，静态检查又抓到一个跨文件的洞

补 `PhotoStore` 那一轮（把「相册 → 压缩副本 → 内容哈希」做成真实现），
本地校验在推上去之前就拦下了一处：

```
FAIL 有 private 存储属性但没显式 init（逐成员初始化器会降级成 private）: PhotoCell, AvatarView
```

**成因：** `PhotoCell` / `AvatarView` 各有一个 `@State private var image: UIImage?`。
Swift 的规则是「只要有一个 `private` 存储属性，逐成员初始化器就降级成 `private`」——
于是这两个组件**只能在同一个文件里被构造**。
`PhotoCell` 当时只被同文件的 `PhotoGrid` 用，看不出问题；
而这一次 `AvatarView` 要跨到 `ProfileEditView` / `SecondaryViews` 里去用，
一跨文件就是 `inaccessible due to 'private' protection level`。

**这条只有在「组件第一次被别的文件用到」的那一刻才暴露**，而那时候错误会报在
**调用方**（新文件）而不是定义方 —— 找起来比看起来费劲。所以规则是：
**组件里有 `private` 存储属性，就必须手写 `init`。** 已在 lint 里固化成回归项。

> 同一轮还顺手补了 lint 的框架表：以前 UIKit 的判据只认
> `UIApplication|UIColor|UIFont|UIGraphics`，**不认 `UIImage`** ——
> 也就是说「用了 UIImage 却没 import UIKit」这种最常见的漏网它一个都抓不到；
> 而 `PhotosUI`（`PhotosPicker` / `PhotosPickerItem`）整个不在表里。
> 这两库和 `UserNotificationsUI` 是同一类坑：**不 import 就是 `cannot find type in scope`**，
> 而那个坑上一轮真在云端炸过两次。

### 3.6 加「轻提示」时，静态检查同时报了一真一假

补 15/16 引导、22 轻提示、26 删除确认、27 回收站那一轮，lint 报：

```
FAIL 有 private 存储属性但没显式 init（逐成员初始化器会降级成 private）: ToastBar, ToastLayer
```

**这两个里只有一个是真的。**

- **`ToastLayer` —— 真问题。** 它有一个 `@ObservedObject private var center`，
  而且要被 `HomeView` 跨文件构造 → 必须手写 `init() {}`。
- **`ToastBar` —— lint 自己看错了。** 分块正则写的是 `/\n(?=struct |enum |extension )/`，
  **不认 `class`**。而这一轮新加的 `ToastCenter` 是个 `final class`，
  它写在 `ToastBar` 前面 —— 于是分块时 `ToastCenter` 的 private 成员
  被算到了 `ToastBar` 头上，凭空造出一个不存在的问题。

**教训有两层：**

1. **lint 的「分块」规则必须认识语言的全部顶层声明形式**，否则前一个块会把成员「漏」给后一个块。
   已改成 `/\n(?=(?:final )?(?:struct|enum|extension|class|actor) )/`。
2. **修 lint 必须配反例自测**，否则你没法知道它到底是「不报了」还是「永远不报了」。
   做法：临时加文件，前一个 `struct _TmpProbe` 紧跟一个 `final class _TmpHelper`（带 private 成员），
   **必须报 `_TmpProbe`** —— 报不出来就说明修复是假的，跑完删掉临时文件。

> 顺带一个刻意的反向决定：**`ToastCenter` 没有加 `@MainActor`。**
> 加 `@MainActor` 之后，`ToastLayer` 会在属性初始化器里读 `ToastCenter.shared`，
> 那是非隔离上下文，在 Swift 5 模式下会一路发警告 ——
> 而这一轮的 CI 目标是「注解区只留编译器的声音」。用 `Task { @MainActor in … }`
> 把真正需要主线程的那段（3 秒后自动收起）单独包起来就够，不必给整个类上锁。

### 3.7 做 21 屏时撞出来的一个真 bug：新建记录会变成好几条

这一条**五套静态校验一条都拦不住**，编译器也拦不住 —— 它只在真机上手打一段字才现形。

**症状：** 新建一条记录，标题分两次打完（中间停超过 0.8 秒），列表里出现两条几乎一样的记录。

**成因：** `RecordEditorView` 用 `@Query` 找「正在编辑的那条」：

```swift
let target = editingID.isEmpty ? "\u{0}__new__" : editingID   // 哨兵 id
_existing = Query(filter: #Predicate<Record> { $0.id == target })
```

哨兵 id 是**为了让 `#Predicate` 绕开捕获变量的限制**（这个理由本身是对的，见 3.2 节）。
但 `Record.newID()` 生成的是随机 id：

```swift
static func newID() -> String { "r_" + UUID().uuidString.prefix(6).lowercased() }
```

**两者永远不相等。** 于是 `ctx.insert(新的记录)` 之后，`existing` 依然是空的，
`record` 一直是 `nil` → 下一次自动保存又走「新建」分支 → 再插一条。

**为什么不容易发现：** 自动保存是 0.8 秒 debounce，而人在打字时通常不会停那么久。
但只要停下来想一下（而这正是记事时最常见的行为），就会多一条。
而且多出来的记录内容几乎一样，看起来像「同步出错了」，一点也不像「新建写错了」。

**改法：新建出来的那一条自己拿引用，不指望查询能找回它。**

```swift
/// `@Query` 的谓词在 init 里就定死了，新插入的记录不会出现在 `existing` 里。
@State private var created: Record?
private var record: Record? { created ?? existing.first }
```

**能沉淀的规则：** 用哨兵值绕开 `#Predicate` 限制的时候，
**这个哨兵值必须同时是「真的会被用到的 id」**，否则插入之后查不回来。
更一般的说法：**「查回来」和「自己拿着」是两种不同的可靠性** ——
查回来依赖框架的谓词/缓存/刷新时机，自己拿着只依赖赋值。
不可恢复的破坏性副作用（比如再插一条记录）不该建立在「查得到」上面。

> 同一轮还顺手改掉一处与画布不一致：**照片字段只该出现在编辑态。**
> 画布 03（新建）、21（起手引导）的字段都是「分类 → 标题 → 内容 → 标签 → 提醒」，
> 没有图片；只有 34（记录配图 · 插入照片）在标题和内容之间插了这一段。
> 三处一致，说明设计的意思是「新建时先记文字，照片之后回来补」。
> 代码原来是无条件显示的，现在改成 `if isEditing`。


### 3.8 第四轮：一次**自己造成的**编译错误（本地九份全绿也没拦住）

**现场。** 第三轮（§3.6 那条「读 `Photo` 对象」）的包 `62e02e4` 推上去后 CI 直接 red：

```
HerInfo/Models/Models.swift:979:5: error: '@discardableResult' attribute cannot be applied to this declaration
HerInfo/HerInfoApp.swift:153:26: warning: result of call to 'cleanupExpiredTrash(in:)' is unused
```

**两条诊断是同一处失误的两面。** 第三轮往 `Models.swift` 里插
`backfillPhotoHashes` 时，把**旁边那个函数**（`cleanupExpiredTrash`）的
文档注释 + `@MainActor` + `@discardableResult` 留在了插入点上方 ——
属性于是落到新插的 `static let photoBackfillKey` 头上（**error**），
原函数丢了属性、`Int` 返回值没人接（**warning**）。
看到「属性报错 + 邻近函数报未使用返回值」这两条同时出现，就该想到「它们本来是一家的」。

**修法**：把「文档注释 + 两个属性」整块搬回 `cleanupExpiredTrash` 声明之前。
**纯位置搬家，一个字都没改**（10 行，文件字节数不变）——
用行号切片搬，不用长文本替换（长注释最容易差一个字符）。

**为什么九份脚本没拦住。** 这类问题纯粹是**语法形状**，判据是「这一行归谁」，
而文本匹配看不见「归属」。当时九份全绿，一条都不报。
> 顺带一条已经在 §5 写着的话现在有了实例：`check-symbols.py` 自己就写着
> 「**不代替编译**」。凡是文档里写「本地全绿」，都必须补一句
> 「绿的是这十份，不包含编译」。

**补的守卫（第 40 项「不悬空属性」）**：扫「**纯属性行**组成的块」
（剥掉属性后整行为空的那种），到下一个有效行时判 ——
① 那个有效行不是声明 → 悬空；② 块里含 `@discardableResult` 而落到非 `func` → 报。

> **判据必须收窄到「纯属性行」。** 第一版粗糙扫描把
> `@State private var x = false`（属性与声明同行）、
> `@Query(filter: #Predicate<Record> { … },`（属性跨多行）、
> `init(@ViewBuilder content: …)`（参数里的属性）**全报成了悬空 —— 63 处误报**。
> 误报比漏报更坏：它会让人开始忽略这个脚本。

**反例自测**（`.workbuddy/_p13_negtest.py`）：`A.swift` 全合法 → **必须绿**；
`B.swift` 复刻本次错位（`@discardableResult` 分别落到 `static let` / `static var`）
→ **必须精确报 2 处**。A 绿 / B 红，通过。
> 它和 `_negtest_guard3.py`（11 个「语义等价改写」式反例）**是两码事，别合并** ——
> 那一套刻意不造语法错，这一套必须造。

**可迁移的判据**：**只要动了「别人代码块的边界」，就把相邻的属性行列为第一嫌疑。**
`git diff` 的**行数差**也是线索 —— 这次只搬了 10 行，正常编辑不会「凭空多出 10 行」。

---

## 4. 修复协议（一轮走完）

**第 1 步：拿到编译器输出。** 两条路，选一条：

- **没有 Mac（推荐）** —— 把 `她的信息本-iOS/` **整个目录作为仓库根目录**推上 GitHub，
  `.github/workflows/ios-build.yml` 会自动跑 `xcodebuild`，
  并把 `error` / `warning` **摘成清单**回在 Actions 日志里（不用你自己翻 3000 行构建日志）。
  单次约 3–5 分钟。

  **本地仓库已经建好并提交**（分支 `main`，3 个提交，工作区干净），操作只剩两步：

  1. 仓库已在 GitHub 上建好：**`https://github.com/haohao234/She`**。
     > 它是**公开**的，而且建的时候勾了 `Add a README`。
     > 公开与否只影响谁能看到代码，不影响编译 —— 想改私有的话，
     > 仓库页 `Settings` → 最下面 `Danger Zone` → `Change visibility`，趁代码还没推上去最省事。
  2. **双击 `她的信息本-iOS/push-to-github.bat`**，然后按回车（地址已经配好）。

  脚本自己会做完剩下的事：找 git → 进入仓库目录 → 报本地状态 → 查代理 →
  **先测地址** → 查远程空不空 → 推送。

  > **为什么地址和远程非空这两件事要单独处理** —— 第一次推送失败，
  > 原因正是这两条叠在一起：脚本里配的地址是 `…/For-She.git`（GitHub 上不存在，
  > 报 `Repository not found`），而真实仓库 `She` 里有个自动生成的 `Initial commit`
  > （就算地址对，也会被 `rejected` 挡回来）。
  > 新版脚本在推送**之前**分别探测这两件事，并把 git 的原始输出打出来 ——
  > 失败信息不再是笼统的一句「推送失败」。
  > 每次运行还会留一份 `推送日志.txt`（已加进 `.gitignore`），可以直接发出去定位。

  > **第二次失败是另一码事：网络时通时断。**
  > 日志里是 `Failed to connect to github.com:443 after 21033 ms: Could not connect to server`。
  > 这不是地址问题也不是账号问题 —— 实测同一台机器**同一分钟里**，
  > `git ls-remote` 失败，几分钟后原样重跑就通了。
  > 所以脚本 v3 在「测地址」和「推送」两处都加了**自动重试**：
  > 网络类失败先原地等 4–5 秒重来，最多 3 次，三次都不通才报「网络不通」。
  > 报「网络不通」时也改用「过一会儿重跑 / 换手机热点」开头，
  > 不再让用户去改地址或重新登录 —— 那是完全错的方向。

  完整步骤、每一步的验证点、报错对照表都在 **`她的信息本-iOS编译操作指引.html`**。

  > **为什么是双击脚本，而不是敲命令** —— 这台机器上装的是随工具附带的**便携版 git**：
  > 它不在系统 PATH 里（直接敲 `git` 会提示「不是内部或外部命令」），也没有 Git Bash
  > （那是装了 Git for Windows 之后才有的）。脚本按「便携版 → 正式版 → PATH」依次去找 git，
  > 并用**脚本自身的位置**定位仓库目录，所以不依赖任何环境配置。
  > 也正因为如此，它**必须待在那个目录里**，挪出去就认不到仓库了。

  > **手动方式（备用，脚本跑不起来时才用）** —— 需要一个能识别 `git` 的终端：
  > 先装 Git for Windows，或用完整路径调用本机已有的便携版。
  >
  > ```bash
  > cd "D:/workbwenjian/2026-09-10-14-54-12/她的信息本-iOS"
  > git ls-remote --heads https://github.com/haohao234/She.git   # 先单独测地址
  > git push -u origin main
  > git push -u origin main --force                               # 报 rejected 时用这条
  > ```
  >
  > 推完回到仓库网页 → 点 Actions 标签 → 等「iOS 编译检查」跑完。

  > **最容易做错的一步**：仓库根目录必须是 `她的信息本-iOS/` **本身**，不能是它的上一层。
  > 推错了的话 `.github/workflows/` 就不在根目录，Actions 什么都不会跑
  > —— 而且不报错，它只是「没有工作流」。
  >
  > 首次 `git push` 会弹窗让你登录 GitHub（Git Credential Manager）。
  > 如果它要密码：GitHub 早就不收密码了，得用 Personal Access Token 当密码。

- **有 Mac** —— `brew install xcodegen && xcodegen generate`，打开工程，**⌘B**。
  （不需要手工建 target / 拖文件 / 勾 Compile Sources / 加 App Groups，都在 `project.yml` 里。）

**第 2–6 步：**

2. 把**错误与警告整段复制**贴回来 —— 保留 `文件:行:列` 那一段，
   有 `note:` 补充行也一起贴（Swift 的 `#Predicate` 与并发报错全靠 note 定位）。
3. 我按行改，改完**重跑 `check-swift.js`（27 项）+ `check-project.py`** 保证不回归。
4. 如果报错落在第 3 节之外 —— **那是真错误**，优先修；
   而且多半说明我的静态判据还漏了一类，我会把新判据补进脚本，
   让下一轮不用再靠人眼通读。
5. 一轮跑完再决定要不要第二轮。**如果只出现这三类，通常一轮就收敛。**

> 不需要你贴全量日志。**只要 error 与 warning 的行**就够了，
> `note:` 行比 error 行更有用。

---

## 5. 静态检查已经覆盖到什么程度

十份脚本，都在 **`.workbuddy/checks/`**：

| 脚本 | 跑法 | 管什么 |
|---|---|---|
| `check-swift.js` | `node check-swift.js <目录>` | Swift 源码：配平、引用闭合、路由落点、跨进程契约、防漂移、**不读 `Photo` 对象**、**不悬空属性**（40 项） |
| `check-argorder.py` | `python check-argorder.py <目录>` | 每个成员 `init` 的**标签顺序**必须与属性声明顺序一致（50 个类型 / 85 处调用 / 0 问题） |
| `check-project.py` | `python check-project.py <目录>` | 工程声明：`project.yml` 路径自洽、两份 plist 合法、**分类 id / App Group 跨文件一致** |
| `check-guide.py` | `python check-guide.py <目录>` | 操作指引与推送脚本自洽：路径存在、`.bat` 是 CRLF + **GBK 字节**、标签无悬空（68 项） |
| `check-symbols.py` | `python check-symbols.py <目录>` | **找不到符号**：裸函数调用与 `C/CYC/Typo/S/R/Shadow` 成员逐个对回定义处 |
| `check-handoff.js` | `node check-handoff.js <html>` | 交接文档：章节、37 屏逐条、验收清单（9 项） |
| `check-prototype.js` | `node check-prototype.js <设计稿目录>` | 交互原型：屏段齐全 / META 进索引 / 深链正则 / 32~36 屏节点齐备 / 无锚点嵌套 / 类名无重复 |
| `check-spec.js` | `node check-spec.js <设计稿目录>` | 设计规范页 |
| **`check-globalsel.js`** | `node check-globalsel.js <html>` | 单文件原型里「**全局选择器跨了多个屏**」—— **裸**选择器 + 取单个 + 跨多屏 = FAIL；带值限定 / 带 `.on` 这类状态类 / `querySelectorAll` = INFO，逐条人工确认 |
| **`check-imgrow-live.py`** | `python check-imgrow-live.py` | **唯一会开真浏览器的**：注入探针 → 无头 Edge `--dump-dom` → 断言**屏内交互**（5 项）。补的正是上面 `check-prototype.js` 查不到的洞 |

**跑法（2026-09-15 补，本轮真踩到）**：**先把 `cd` 切到目标目录，参数传 `.`**，
不要传带中文的绝对路径 ——
`check-swift.js` 有一次在 `read()` 处 `ENOENT` 崩掉（`rc=1`），把参数里的
`她的信息本-iOS` 整段丢了，而**同一条命令上一次是跑通的**（MSYS 往原生 exe 传中文参数时的偶发丢失）。
**判读前先看 `rc`，并且要看到结论行**：`rc≠0` 或没有结论行 = **脚本自己没跑完**，
不是「有 FAIL」；用 `grep 结论行` 读一个**已存在的输出文件**时尤其危险 —— 会读到上一次的结论。

`check-swift.js` 跑的是**静态可判定**的那些事 —— **40 项全通过**：

（以下是它的真实输出，按类归并了一下）

（以下是 **2026-09-15 的真实输出**，原样）：

```
Swift 文件 30 个（HerInfo + HerInfoNotification + HerInfoShare）

『结构』
  OK   全部文件的 {} () [] 配平
  OK   无符号重复定义（110 个自定义类型）

『路由』
  OK   Route 的 18 条路径全部有落点: profile profileEdit settings appearance sortPin reminders reminderNew search recordDetail versionHistory recordEdit starterEdit trash exportArchive lock notifyDenied mood cycle

『引用』
  OK   引用的 30 个 View 类型全部有定义或属于系统框架
  OK   框架 import 完整（SwiftData / UserNotifications / CoreLocation / LocalAuthentication / UIKit / PhotosUI / UniformTypeIdentifiers）

『跨进程契约』
  OK   共享契约只依赖 Foundation + UserNotifications（不把 SwiftData / SwiftUI 带进扩展）
  OK   分类 id 与扩展 Info.plist 一致（HI_REMIND）
  OK   扩展 Info.plist 四个关键键齐全（含最容易漏的 UserInteractionEnabled）
  OK   五个情绪档 rawValue 一一对齐（happy,tired,down,angry,calm）
  OK   12 个共用色值（4 分类 + 主色三档，各浅深两档）只在共享契约里定义一次

『分享扩展』
  OK   分享契约只依赖 Foundation（不把 SwiftData / SwiftUI 带进扩展）
  OK   共享容器只有一处定义（分享契约引用 HINotify.appGroup，不重写字符串）
  OK   四个分类 rawValue 一一对齐（like,trait_,care,hate）
  OK   分享扩展 2 个源文件里一个字面色值都没有
  OK   分享扩展没有 @main（入口由 NSExtensionPrincipalClass 指定）
  OK   分享扩展只丢收件箱、自己不开数据库（与打标收件箱同一模式）
  OK   分享扩展挂在 com.apple.share-services 上
  OK   分享扩展的 CFBundlePackageType == XPC!
  OK   分享扩展声明了 NSExtensionActivationRule

『防漂移』
  OK   通知样式的颜色全部取自共享契约，一个字面色值都没有
  OK   设计令牌层引用契约取色（Cat 9 处 / Primary 7 处）
  OK   PDF 导出取的是契约里的色（屏幕 / 通知 / PDF 三处同色）

『曾经的错（回归检查）』
  OK   UNUserNotificationCenter.add 全部带 try/await（add 是 async throws）
  OK   带 private 存储属性的组件都有显式 init（跨文件调用不会报 inaccessible）
  OK   地理围栏上限 20 已写成常量
  OK   图片上限 9 已写成常量
  OK   每个 @AppStorage 开关都至少有一处真读（12 个键）
  OK   已无残留的 `tiny:` 调用 · 胶囊三档统一走 `PillSize`
  OK   胶囊三档齐备 · 盒高写死 · textTint 在位
  OK   主色投影（规范 L4）已收进 Shadow.primaryGlow* · 调用点没有重写数字
  OK   自动保存的 Task 被掐住（存活标记 + context 预取 + onDisappear 兜底）
  OK   周期页 reschedule 走显式 context（await 之后不再读 self.ctx）

『不读 Photo 对象（2026-09-15 第三轮定稿）』
  OK   全工程不读 record.photos 关系（只有 Models.swift 里那一行声明）
  OK   全工程不 fetch Photo 对象
  OK   Record.photoHashes 标量在位（在 Record 类体内核过）
  OK   两个配图写入点（编辑器保存 / 历史版本恢复）都写标量
  OK   启动守门齐备（begin 在开库之前 / finish 延后复位 / 三级退路 + 如实告知）
  OK   回填排在清孤儿图片之前，且清理以「回填已完成」为前提
  OK   容器兜底把 .store / -shm / -wal 三个文件一起搬

『不悬空属性（2026-09-15 第四轮：包编译不过的那类错位）』
  OK   没有悬空属性（@MainActor / @discardableResult 都紧贴它们的声明）

✓ 全部通过（40 项）
```

**它守不住的是：** 类型推断的最终结果、宏展开、actor 隔离、以及
「这段代码跑起来对不对」（比如时机、边界、生命周期）。后面这一半，只有编译与真机。

---

## 6. 编译通过之后，手动过一遍这 22 条

### 6.0 真机验之前：包怎么进手机（这是**缺的那一环**）

> ⚠️ **本节曾经写着「`HerInfo.app` 装不进真机，流水线连 artifact 都没上传」—— 那已经过时了。**
> 那条流水线当时只负责「让编译器说话」。现在它多了两步
> （看 `.github/workflows/ios-build.yml` 里的**「打成可侧载的 IPA」**与**「上传 IPA」**）：
> **打成可侧载的 IPA**（`HerInfo-<短 sha>-unsigned.ipa`，就是「`Payload/` 里放 `.app`」再 zip）
> 与**上传 artifact `HerInfo-ipa`**，再加一道**拆包自检**（扩展够不够 2 个、版本键在不在、
> 有没有夹带 `*.debug.dylib`）—— 这四样任一出问题 CI 直接红，坏包不会变成你能下载的 artifact。

**当前用的是 C 路**（Sideloadly + 免费 Apple ID）。这条路已经跑通：截至 `461e299`
多轮包都装上并跑过真机，用户反馈的配图闪退就是这么发现的。
**但 C 的 App Group 卡点依然在**（见下），所以「装得上」不等于「22 条全能验」。

三条路，代价差很多：

| 路 | 需要什么 | 能验到哪 | 代价 / 风险 |
|---|---|---|---|
| A. 借一台 Mac | 任意 Mac + Xcode + 有线连手机 | **全部 22 条** | 最省事、最可能一次成功。免费 Apple ID 即可（**7 天过期**）；下次改代码还得再借一次 |
| B. 付费开发者账号 | 账号（¥688/年）+ 证书与描述文件 —— **这两样在 Windows 上也能生成** | **全部 22 条**，且以后每次改代码由 CI 直接产出签名 IPA | 唯一能形成闭环的路：证书 + 描述文件放进 Actions secrets，`archive + exportArchive` 出 IPA，再在 Windows 上用 Sideloadly / 3uTools 装。**以后不需要再有 Mac** |
| **C. Sideloadly + 免费 Apple ID（**在用**）** | Windows + Sideloadly（装机前要装**网页版** iTunes + iCloud，商店版必须先卸） | **不确定**，见下面的硬卡点 | 不花钱；7 天过期（自带自动续签）；**App Group 那三条验不了**。**逐步骤做法见操作指引 STEP 05** |

**C 的硬卡点：App Group + 两个扩展。** 这个 App 的跨进程通信**全靠 App Group**
（`group.com.herinfo.app`）：通知里打的标、分享进来的内容，都是先写进 App Group，
再由主 App 的 `drainInbox` 落库。而：

- Apple 对「个人团队」的官方限制是 **10 个 App ID / 3 台设备 / 每个 App ID 与描述文件 7 天过期**，
  并写明**要构建更进阶的权限能力必须加入付费的 Developer Program**；
- 实测报告里 App Groups 在免费账号上**失败率很高**，报错长这样：
  `None of the valid provisioning profiles allowed the specified entitlements: com.apple.security.application-groups`
  —— 免费账号在网页端**看不到 Certificates, IDs & Profiles**，所以 Xcode 那个「Fix Issue」也帮不上；
- Sideloadly 的**自定义 entitlement 功能明确只对付费账号开放**（Patreon 专属）。

→ 走 C 的话，**先验第 6~9 条（配图管线，完全不碰 App Group）**；
App Group 那三条（1 / 2 / 22）大概率验不了。**看到上面那个报错不用怀疑代码 —— 是账号能力的问题。**

**另外两处可能撞车（也都不是代码问题）：**

- **Bundle ID 是全局唯一的**：`com.herinfo.app` 若已被别人注册，免费账号只能改 id；
  而 Sideloadly 的「改 Bundle ID」**不保证连带改写两个 appex 的 id** ——
  扩展 id 必须以主 App 的 id 为前缀（现在是 `com.herinfo.app.notification` / `com.herinfo.app.share`，
  `check-project.py` 锁着这个前缀关系），前缀对不上**扩展就不加载**。
- **App Group 的 id 同样要注册得到**：`group.com.herinfo.app` 也要能落到你的账号下。

**C 路上实测撞到的第一个报错：`Sideloading failed! Invalid file`**（2026-09-11）

配上 `Install failed: Guru Meditation f65043@1006:23a71c`。那句话的含义是
**「我读不出我手上这个东西」**，不是「苹果拒绝了你的包」；那串十六进制是 Sideloadly
自己的内部编号，**不是诊断信息**（同一句话在不同编号下都出现过）。成因有六七种，按这个顺序排：

1. **拖错一层** —— GitHub 的 Artifacts 自己是个 zip，里面才是 `.ipa`。
   拖 `HerInfo-ipa.zip`、或把那个 zip 改名成 `.ipa`，都会报这个（里面没有 `Payload` 那一层）。
   **最常犯的一条。**
2. **Anisette 没切到 Local** —— Sideloadly 默认用官方那个**远程** Anisette Server
   （要捐赠才能用、国内常连不上），连不上的表现就是**签名还没开始就失败**，
   而**它报的也是 `Invalid file`**。高级选项里点 `Download` 下载本地服务，
   **再在左侧选中 `Local`** —— 只下载不切换等于没做。
3. **文件没下全** —— 中断过的下载能打开、解压时却缺东西。重下；下完先自己解压一遍，
   能解出 `Payload` 文件夹才算好。
4. **路径带中文或空格** —— 挪到 `C:\ipa\` 这种纯英文目录再拖。
5. **Sideloadly 版本太旧** —— 苹果那边一改，旧版本就读不懂返回的东西。
6. **换网络** —— 关掉代理、或用手机热点。这个十六进制码在国外论坛有**一模一样**的记录：
   当事人试遍手里所有 IPA 都没用，**最后是换网络解决的**。
7. **最后一条退路：去掉扩展再试** —— 高级选项里的 `Remove App Extensions`
   （或解开 IPA、删掉 `Payload/HerInfo.app/PlugIns` 再压回去）。
   **代价**：通知打标与分享两条线**彻底**没得验；但在免费账号下它们本来就是哑的，
   所以这一步换来的是「主 App 至少装得上」。

**顺带查出包自己有两个真毛病（已修，且现在有断言守着）：**

- **三个 bundle 都缺 `CFBundleVersion` / `CFBundleShortVersionString`。**
  根因：三个 target 都开 `GENERATE_INFOPLIST_FILE`，而
  `MARKETING_VERSION` / `CURRENT_PROJECT_VERSION` **从来没设过** ——
  Xcode 拿空值去生成，于是这两个键**干脆不存在**，而且**不报错**。
  而 `CFBundleVersion` 是安装校验的必填项。已在 `project.yml` 的全局 settings 里补上。
- **那是 Debug 配置的构建。** Xcode 16 给 Debug 配置默认开了 `ENABLE_DEBUG_DYLIB`：
  包里躺着 `HerInfo.debug.dylib` **7.7 MB**，主可执行文件只剩 **71 KB 的壳**，
  靠 `@rpath` 在运行时加载它 —— 侧载要多签一道、真机启动还得路径对得上。
  已把流水线改成 `-configuration Release` + 显式 `ENABLE_DEBUG_DYLIB=NO`。

**并且加了一步拆包自检**（`校验 IPA 内容`）：扩展没嵌够 2 个、版本键缺失、
扩展版本与主 App 不一致、包里出现 `*.debug.dylib` —— **这四样里任何一样出问题，
CI 直接红，坏包不会变成用户能下载的 artifact**。
这类问题的共同点是「**编译全绿、包看起来也正常，装的时候才拒**」，
**没有断言守着就一定会再次发生**。

**第 27 轮的实测结果**（把 artifact 下下来自己拆开验的，不是复述 CI 的话）：

| 项 | 修之前（第 26 轮） | 修之后（第 27 轮） |
|---|---|---|
| 三个 bundle 的版本键 | **一个都没有** | `1.0 (1)` × 3 |
| 调试动态库 | `HerInfo.debug.dylib` **7.7 MB** | **无** |
| 主可执行文件 | **71 KB**（只是个壳） | **4.4 MB**（真身） |
| IPA 体积 | 2.07 MB | **1.15 MB** |
| 构建配置 | Debug | **Release（`-Os`）** |
| 顶层结构 / 扩展入口键 | （这两样本来就没问题） | `Payload` 唯一、无 `__MACOSX`；两个 appex 的 `NSExtensionPointIdentifier` 都对 |

**真机上撞到的第二个报错 —— 结论是「包是旧的」，不是新 bug**（2026-09-11 17:35 查实）

装的时候被 iOS 的安装服务拒掉，原文（用户贴回来的）：

```
There was an issue during installation: 0: APIInternalError
(Error Domain=IXErrorDomain Code=2 "Failed to create app extension placeholder for
/var/mobile/tmp/ExtractedApps/temp.…/Payload/HerInfo.app/PlugIns/HerInfoShare.appex"
… NSUnderlyingError … Code=-17 "bundleVersion must be set in placeholder attributes
for an app extension placeholder")
```

**查法（把两代 artifact 都下下来拆开对）：**

| | 旧 artifact `HerInfo-4794fed-unsigned.ipa` | 当前 artifact `HerInfo-eb7cca9-unsigned.ipa` |
|---|---|---|
| 体积 | **2.00 MB** | **1.15 MB** |
| 主的 `CFBundleVersion` | **不存在** | `1` |
| 两个 appex 的 `CFBundleVersion` | **不存在** | `1` |
| 调试题动态库 | 6 个（每个 bundle 各 2 个） | **无** |

- **报错只点名 `.appex` 是有原因的**：**主 App 的 placeholder 允许缺版本键，扩展的不允许**。
  三个 bundle 都带病的旧包，先被拦下的就是扩展 —— 所以「只怪扩展」不代表主 App 是好的。
- **处置是换包，不是改设置**：不用重装 iTunes、不用关 Anisette、也不用 Remove App Extensions。
- **这类「用户拿到旧 artifact」有结构性原因**：artifact 留 30 天，新旧包在同一页里长得一样，
  只差文件名里那 7 位提交号。→ 指引 STEP 05 因此加了一条「**先认包**」：
  **1 MB 出头是对的，2 MB 左右是旧包**；并且把「点最上面（最新）那次 run」写成硬要求。
- **实测复现两次（第 3 次用户真机、第 4 次仍是它）**，用户装的一直是桌面上的
  `HerInfo-4794fed-unsigned.ipa` —— 可见「旧包留在桌面/下载夹里」是**复发点**，
  不是一次性事故。→ 确证手段写进指引：**看 Sideloadly 第一行**
  `Using IPA file: …\HerInfo-<提交号>-unsigned.ipa`，那一行是「我装的是哪个包」的唯一确证；
  并且要求**认出旧包就直接删掉**（留着必被再拖一次）。
- 用户贴的日志末句 `Install failed: Guru Meditation 556260@79:6edd68
  __init__() missing 1 required positional argument: 'orig'` 是
  **Sideloadly 处理该失败时自己抛的二次异常**，不是第二个独立故障；真原因在它上一行。
- **报错里 `com.herinfo.app.6LS7VTC3LX.share` 那串十位后缀不是写错 id** ——
  那是 Sideloadly 的「自动 Bundle ID」加的唯一后缀（免费账号默认开）。
  它把主 App 和两个扩展**一起**加上了同一后缀，前缀对得上，是对的。
  但社区里最常见的形态是**只改主 App、没改扩展**，报
  `… does not match required prefix of …` —— 那一档的处置是**关掉 `Use automatic bundle ID`**
  再手动指定 id。

**这三条闸门已经在新包上连续两次实测通过：**

| run | 提交 | 结论 | 第 9 步 | 产物 `HerInfo-ipa` |
|---|---|---|---|---|
| **#29** | `6ab3ec5` | `completed / success`（10 步全绿） | success | 1,201,314 bytes ≈ **1.15 MB** |
| **#30** | `7b4d2ce` | `completed / success`（10 步全绿） | success | 1,201,304 bytes ≈ **1.15 MB** |

也就是说：**「扩展数量 = 2」「扩展版本与主 App 一致」这两条断言不是写在文档里的空话，
它们真的会在真包上跑，跑不过 CI 就红。** 用户下一次拿到的包，体积对得上 1.15 MB 就可以放心装。

> **artifact 里装的是什么**（这条决定了用户该拖哪一个）：
> 解开 `HerInfo-ipa.zip`，**里面只有一个文件** ——
> `HerInfo-<提交号>-unsigned.ipa`。要拖进 Sideloadly 的是**它**，
> 不是那个 zip，也不是解开后看到的 `Payload` 文件夹。

> **一句话**：清单（下面那 22 条）早就写好了，缺的是**把包送进手机**。
> 路 A 最省事、路 B 能一劳永逸、路 C 先别指望 App Group 那几条。
>
> **路 C 的逐步骤做法在 `她的信息本-iOS编译操作指引.html` 的 STEP 05**
> —— 包括「网页版 iTunes / iCloud 怎么装」（Sideloadly 不认商店版）、
> 「Artifacts 必须登录 GitHub 才能下」、**「先认包：约 2 MB 的是旧包」**、
> **「下下来的要解压两次、拖错一层就报 `Invalid file`」**、
> **「先切本地 Anisette Server」**、手机上那两个漏了就「装上了却打不开」的开关
> （开发者模式 / 信任证书），以及**按报错原文排的排查表**（`Invalid file` 七条 /
> `app extension placeholder` 的 bundleVersion / `application-groups` / 装上了但扩展是哑的）。
> 那条流水线现在也已经**会产出可侧载的 IPA**（不签名，交给 Sideloadly 签），
> 并且**会自己拆包自检**（扩展数量 / 版本键 / 版本一致性 / 调试动态库）。

---

前 4 条**不会报错**，只会安静地做错事：

1. 收到提醒 → **长按展开** → 出现配图 + 分类点 + 「第 N 版」+ 五个胶囊
   （没出现 = 分类 id 或 `UserInteractionEnabled` 的问题）
2. 展开态点一个胶囊 → 变「已记下」→ **杀掉 App** → 重进 → 这条打标出现在她的档案里
   （没出现 = App Group 的问题）
3. 收起态看一眼：**应该是系统原生样子**，没有多余留白、没有半截胶囊
4. 导出 PDF → 主色与分类色与通知上的**深色档**不同（PDF 用的是浅色档，这是刻意的）
5. 05 屏「已用 N / 20」在加满 20 个区域后**不再静默增加**

第 1、2 条对应交接文档验收清单的第 13、14 条。

**第 6–9 条是配图管线（`PhotoStore`）的，只能在真机上验 —— 虚拟机、模拟器、
云端 CI 全都测不到「相册选择器」这一步：**

6. **03 / 21 / 34 三处都能点「＋」取图** —— 图片字段从 2026-09-14 起三态都在
   （以前只有 34 有，外面套着 `EditorView` 里那层 `if isEditing`）。
   点「＋」→ 系统相册面板出现（**不是「请求访问所有照片」的授权弹窗** ——
   用了 `PhotosPicker` 就不该出现那个）→ 选一张 → 格子出现真图。
   **再把同一张选一次 —— 不应该变成两格**（内容寻址去重，这是 hash 设计的直接可见效果）。
   → **读不出来的那一张要说话。** 比如相册里还在 iCloud、本机只有占位图的照片：
   以前它是**安静消失**的（格子不出现、没有任何解释，看起来就像「点了没反应」），
   现在应该出一条轻提示「这张图没能读出来，换一张试试」，
   **而且已经写好的文字不能被牵连**。
   → **连点两次「＋」、上一批还在读时又选一张**：后选的那张不能被前一批清掉。
   → **一次连选 5–9 张（专门挑几张 48MP 的大图）不能闪退、不能卡死。**
   这三句是 2026-09-13 补的，对应用户报的「导入图片、准备保存时导入失败然后闪退」。
   写入侧以前收的是 `UIImage`，而 `UIImage(data:)` 是**懒解码** ——
   真正的解码发生在 `PhotoStore` 那句 `draw` 上，一张 48MP 原图那一刻要占近 200MB，
   选几张就是几份；而且 `Task.detached` 跑的那条协作线程，自动释放池可能整批都不清一次。
   现在解码交给 `CGImageSourceCreateThumbnailAtIndex`（只解长边 ≤ 2048 那一份），
   每张图处理完就用 `autoreleasepool` 把临时对象放掉。
   **这一条只能真机验** —— 模拟器上没有真实相册、也没有真的内存压力。

   → ⚠️ **2026-09-15 真机又崩了一次：另一个根因，和内存无关。别把上面那段当万能解释。**
     现场是用户报的「导入图片点保存后还是崩溃闪退」，附了崩溃日志
     `HerInfo-2026-09-15-102524.ips`。逐条判读：
     · `exception.type = EXC_BREAKPOINT`、`signal = SIGTRAP`、`termination.code = 5`
       → **Swift 运行时陷阱**（`fatalError` / 断言那一类），
       **不是**内存不足被系统杀（那种是 `EXC_CRASH (SIGKILL)` + jetsam 记录）。
       **所以上面那段「48MP 解码占内存」的分析对这一条不成立** —— 那是 09-13 那个 bug，
       这个不是它的遗留。
     · 主可执行的 `slice_uuid = 0120fa19-fc3f-3fd4-8f36-8822939b500a`
       → 与当时最新包 `497aa9d` 的 Mach-O `LC_UUID` **逐字符相同**
       （比对方法：读 IPA 里 `Payload/HerInfo.app/HerInfo` 的 `LC_UUID`）。
       **所以崩的确实是最新代码，不是「用户装的是旧包」。**
     · 故障线程是主线程；栈的可用部分是
       `save(bump:)` → SwiftData 内部（3 帧）→ 调用者是
       **`closure #1 in RecordEditorView.scheduleAutoSave()`** ——
       **崩的是那条 0.8 秒的自动保存，路径上根本没有手动保存**。
       （更外层那两帧标的是 `PhotoCell.body` 的闭包，那是编译器给 thunk 的
       符号归属噪声 —— `.task(id:)` 只调 `PhotoStore.load` 读文件，不碰 SwiftData。）

     ⚠️⚠️ **2026-09-15 复盘：下面这条「Task 比这一屏活得久」的判断是错的 —— 它不是这次崩溃的原因。**
     写在这里是因为当时确实按它改了代码，那些改动本身有价值
     （它们挡的是另一类真问题，见下面「加固」那一小段），
     但**别再用它解释这一次的闪退**，否则会漏掉真正的根因。

     **推翻它的是一天里更晚的一条日志 `HerInfo-2026-09-15-111030.ips`：**
     · `slice_uuid = 8b839417-d885-3e68-a202-e99b766ab407`
       → 与当时最新包 `4e7e187` 的 Mach-O `LC_UUID` **逐字符相同**。
     · 两次崩溃（10:25 与 11:10）的**调用方不同**：一个是 `scheduleAutoSave`，
       另一个是 `saveAndLeave`（手动点「保存」退出）。
       而 **SwiftData 内部那三帧的偏移字符级一致**（`649084` / `669028` / `338444`）
       → **崩的是同一个动作，跟是谁调它无关。**「调用方活得久不久」自然就不是变量。
     · 把 `save` 反汇编到崩溃返回地址（函数内 +5344）→ `bl` 的目标符号是
       `PersistentModel.getValue(forKey:)`（Sequence 版）—— 也就是**读 `$0.hash`**。
     · 更早那个包 `497aa9d` 的崩溃点解析出来是**同一个符号** → 交叉坐实。

     **真根因（第三稿，也是最终稿）：只要读取经过 `Photo` 这个模型对象，就有崩的余地。**

     > ⚠️ **这一条改过三次，前两次都是错的。把过程留着 —— 它比结论值钱。**
     >
     > | 稿 | 当时的判断 | 怎么被推翻的 |
     > |---|---|---|
     > | 一 | 自动保存的 Task 比这一屏活得久 | 11:10 的日志里调用方是**手动保存**，照样崩 |
     > | 二 | 墓碑对象：`ctx.delete` 没摘关系，读它才崩 | 按这一稿改了（先摘关系再删）→ **11:49 那份日志照样崩在同一个地方** |
     > | 三 | **读 `Photo` 这个模型对象本身** | 三份日志的 trap 地址逐字节相同，而调用方两次都不同 —— 共同点只剩「读 Photo」 |
     >
     > **第二稿为什么也不够：** 它把病根当成「**读关系**」，于是把读取改成
     > 「从 `Photo` 全表 fetch」。看着换了一条路，其实还是同一个动作
     > （读那个对象），所以照崩。**判据从来不是「读关系还是读表」，
     > 而是「读不读那个对象」。**

     **三份日志的硬证据（这是唯一能定案的东西）：**

     | 日志 | 包（`slice_uuid` 前 8 位 / 提交号） | 调用方 | `exception.codes[1]` 落到 SwiftData 的偏移 |
     |---|---|---|---|
     | 10:25 | `0120fa19`（`497aa9d`） | `scheduleAutoSave` → `RecordEditorView.save` 读 `target.photos` | `+0x9e77c` |
     | 11:10 | `8b839417`（`4e7e187`） | `saveAndLeave` → 同上 | `+0x9e77c` |
     | 11:49 | `9d96428d`（`1341220`） | `bootstrap` → `ReminderService.photoIndex` 读 `Photo.hash` | `+0x9e77c` |

     > **`exception.codes[1]` 就是 trap 那条指令的绝对地址。**
     > 把它减去 `usedImages` 里 SwiftData 的 `base`，三次得到**同一个数**：
     > `0x9e77c`（十进制 649084 —— 也正是第一帧的 `imageOffset`）。
     > **三次崩在同一条指令上，而三次的代码路径互不相同。**
     > 这个比对比看栈有用得多：**栈只告诉你「谁调用的」，
     > `exception.codes` 告诉你「崩在哪」。**
     >
     > 反过来说，这也是上一轮为什么没修好：当时只比了「SwiftData 三帧偏移一致」，
     > 于是把结论停在「崩的是同一个动作」，然后去猜那个动作是什么 ——
     > 猜成了「读墓碑」。**同一份数据摆在面前，问法不同，答案就不同。**

     **这一轮的修法：不读了。**
     配图的真相从 `Record.photos` 关系**搬到 `Record.photoHashes` 标量数组** ——
     和 `Revision.photoHashes` 完全对称（那边从第一天起就是标量，
     「历史版本能这么存，当前状态当然也能」）。
     搬完之后，读配图只是读一个 `[String]`：没有 `order` 要排、
     没有行要 materialize、也就没有任何崩的余地。

     **改了哪些地方（读取侧全部改走标量）：**

     | 位置 | 原来 | 现在 |
     |---|---|---|
     | 列表卡片「📷 N」 | `record.photos.count` | `record.photoHashes.count` |
     | 详情页九宫格 | `record.photos` 排序取 hash | `record.photoHashes` |
     | 编辑器打开 | `r.photos` 排序取 hash | `r.photoHashes` |
     | 编辑器保存 | 「先摘关系再删 + 重排 order」整段 | `target.photoHashes = Photo.uniqueHashes(…)` 一行 |
     | 历史版本恢复 | 同一段 | `r.photoHashes = Photo.uniqueHashes(v.photoHashes)` 一行 |
     | 启动：提醒配图索引 | `fetch(FetchDescriptor<Photo>())` | 从 `records` 的标量读 |
     | 启动：孤儿图片引用全集 | 同上 | 从 `records` / `revisions` / `avatar` 的标量读 |
     | 导出（三处） | `r.photos` | `r.photoHashes` |

     **`Photo` 表退成只读的历史数据**：不再写、不再读，只给老库回填用。
     `Record.photos` 那个关系**保留声明但一次都不读** —— 删它是一次结构性迁移
     （要在所有老设备上冒「容器打不开」的险），而留着只是多一列没人读的数据。

     **老库的配图靠一次性回填补回来**（新增的 `Record.photoHashes` 在老库上是空的）：
     来源取 **`Revision.photoHashes`**（版本号最大的那条快照）——
     它是标量，读它不碰 `Photo`。**不用 `Photo` 关系回填**，理由同上：那条路就是崩点。
       · 只做一次（`hi.backfill.photoHashes.v1`）—— 每次都跑的话，
         用户主动清空配图后会被原样加回来，那比「看不到图」更糟；
       · **`purgeOrphans` 以「回填已完成」为放行条件** —— 回填前 `photoHashes` 是空的，
         那时扫一遍等于把用户**所有**配图当孤儿删掉，而且不可恢复。

     **还加了保底（新文件 `Services/LaunchGuard.swift`）。**
     代码层面的判断已经连错两次，这一版不再赌第三次，改为**接受「可能还有没想到的地方」**：
       ① `begin()` 排在容器建立**之前**，把「这次启动了」写进
          `Application Support/launch-state.json`（`.atomic` 写文件 ——
          崩溃发生在启动后 100 多毫秒，UserDefaults 的异步落盘赶不上）；
       ② 上次的标记还没清 → 这次走**保守启动**：跳过回收站清理 / 提醒重排 /
          孤儿图片清理，只保证界面能开（**收件箱不跳** —— 那是用户刚给的信号）。
          这三件的共同点是「晚一次没有后果」，而它们都要读模型对象；
       ③ 保守启动也崩 → 把库整份（`default.store` + `-shm` + `-wal`）挪到
          `broken-<时间戳>/`，新建空库，**并如实弹一句「旧数据未删」**；
          只有连新建都失败才 `fatalError`；
       ④ `finish()` **延后 5 秒**才复位标记 —— 用户报的「一点进去直接闪退」
          很可能落在首屏渲染时（那已经在 `bootstrap()` 之后），
          紧跟启动流程清标记的话，这类崩溃永远记不下来。

     **上面那三道旧加固仍然保留**（`save(bump:using:)` / `guard live.alive` /
     两颗出口按钮与 `.onDisappear` 都 `saveTask?.cancel()`）——
     它们挡的是「任务比页面活得久」那一类，是**另一个**真问题，别顺手删掉。

     **回归判据（只能真机验；前三条与上一版相同）：**
       → 导入图片 → **立刻**点「保存」→ 不能崩。
       → **杀掉 App、重新打开** → 必须能开（这一条是 11:10 那次报的）。
       → 反复「加图 / 删图 / 换图 → 保存」若干轮，再杀掉重开 → 仍要能开。
       → 编辑一条记录 → 从 31 屏恢复一个历史版本 → 不能崩。
       → **打开一条老记录，应该仍看得到原来的配图**（这一条验的是回填）。
       → 升级后再看列表卡片的「📷 N」→ 条数应与升级前一致。

     `check-swift.js` **36 → 39 项**，那一节整节换成「**不读 Photo 对象**」七条守卫：
     不读 `.photos` 关系 / 不 `fetch` Photo / `Record.photoHashes` 在位 /
     两个写入点写标量 / 启动守门（含 `finish` 必须延后）/ 回填先于清孤儿 /
     三个库文件一起搬。
     **另有反例自测 11/11**（`.workbuddy/_negtest_guard3.py`）——
     期间抓到一条真·假检查：守卫③原来在全文件范围搜 `var photoHashes: [String]`，
     而 `Revision` 上有同名同类型的属性，**把 `Record` 那个删掉它照样绿**；
     锚点已收窄到 `Record` 类体内。

     → ⚠️ **同一族的第二处，2026-09-15 顺手一起修了（`4e7e187`）：周期页的提醒重排。**
       这一处**比上面那处更危险 —— 它的窗口是「秒」级，不是毫秒级**：
       `CycleView` 的 `reschedule()` 第一句是
       `await ReminderService.shared.requestNotificationPermission()`，
       它会等**系统授权弹窗**（用户得点一下），可能持续好几秒；
       而 `self.ctx` 是在 await **之后**才读的。
       用户在这几秒里把那个 sheet 关掉，这一屏就没了 —— 等 await 回来再读 ctx，
       拿到的同样是「没被安装过」的默认值，拿它 fetch / insert 就崩。
       （`CycleView.save()` 里也有一处同型：`Task { rescheduleAll(context: ctx, …) }`
       紧跟着一句 `dismiss()`。）
       修法与上面**完全同一条**：**调用方在还看得见这一屏的时候把 context 取出来传进去。**
       `reschedule()` 改成收 `_ context: ModelContext`，6 处调用点各自先
       `let context = ctx`；`notifyOn` / `leadDays` / `notifyHour` 一并提前取好
       （它们是「跟着这一屏的东西」，理由同一条）。
       **回归判据（只能真机验）**：进「经期提醒」设置页（会弹通知授权）→
       **在弹窗刚出来时就把这一页关掉** → 不能崩。
       `check-swift.js` 已加守卫（`reschedule` 必须收 context、全文件不得出现裸调用）。

   **下面两条是 2026-09-14 补的 —— 新建态有「＋」之后才出现的路径，以前从没验过：**
   → 新建态（03）**只加图、一个字都不写**，点「完成」→ 列表里应该真有这条记录，
     点进去图也在。**不用改「没写内容就不建记录」那条守卫** ——
     `snapshot` 里本来就含 `photoHashes`，所以「只有图没有字」也算写过东西。
   → ⚠️ **选图这个动作本身就已经把记录建出来了 —— 后面点不点「完成」都一样。**
     这一条原来写的是「选图后点『取消』→ 列表里不该多出一条记录」，
     **那句话是错的**（2026-09-13 复核后改正）。真实链路：
     `onChange(of: photoHashes)`（`EditorView` 的 body 里）→ `scheduleAutoSave()` →
     0.8 秒后 `save(bump:using:)`。新建态那条守卫
     （`save` 里的 `guard snapshot != lastSnapshot`）**只在「和刚打开时一模一样」时才拦住**，
     而 `photoHashes` 已经变了 → **记录当场落库**。
     「取消」的实现在 `EditorView` 顶行那颗按钮里，只有一句 `path.removeLast()`，
     **不删任何东西**（2026-09-15 起它改成「先 `saveTask?.cancel()` 再 pop」——
     理由见上面那段闪退，取消自动保存与删数据是两件事）。
     → 所以应该看到的是：**列表里多出一条记录**（只有图、没标题、没正文）。
       它是**一条正常记录**，那张图**不是孤儿** —— 记录正引用着它，
       `purgeOrphans` 不会收它（原来那句「文件成孤儿、由 purgeOrphans 收」也错了）。
     > ⚠️ **这一段原来引的是行号（`EditorView:284` / `:600` / `:486` / `:107`），
     > 2026-09-15 已全部改成按符号名找。** 理由很实际：改一次代码它们就集体失准，
     > 而**失准的行号比没有行号更坏** —— 它把人指到一段无关的代码上，
     > 还让人以为「这里核对过了」。**按函数名 / 字段名索引，别抄行号。**
     → 接着再新建、选同一张图，会正常复用同一个 hash（文件确实还在）。
     → 顺带必须看一眼：这种记录在列表里的**第一行是空的**
       （`RecordCard` 直接渲染 `record.title`，全项目搜不到兜底；
       详情页 `BrowseView` 同样），卡片上只剩右侧时间和「📷 1」。
     → **2026-09-13 已定案（三处）**：
       ① 编辑器顶行那颗按钮**改名「返回」**（03 / 08 / 21 / 34 四屏，画布 + 原型 + PNG
       一起换）—— 这一屏本来就自动保存，状态条自己写着「已自动保存 · 刚刚」，
       「取消」在那儿是一句假话。**行为一个字没动。**
       ② 22 屏那颗「撤销」**接上了**（原来 `saveAndLeave` 只 `show(text)`、没传
       `actionTitle`，`ToastBar` 根本不画它）。落点 `HerInfoStore.discardDraft`：
       **真删掉、不进回收站**；**只配给「刚新建」那一条**，编辑既有记录不给。
       ③ 列表卡片的空标题**保持空白** —— 有空图无标题的记录，卡片上就是时间 + 「📷 N」。
     → **下面三条是本轮新加的验收项（都只能真机验）**：
       ✅ **2026-09-15 已真机验收通过** —— 用户反馈「符合，暂时没什么问题」。
          三条结论：① 撤销后记录真的消失、回收站不多东西 ✓
          ② 编辑已有记录后**不出现**「撤销」✓
          ③ 只加图不写字 → 列表上是**空标题 + 时间 + 「📷 1」**，是预期 ✓
          **原文保留在下面**，作为以后回归时的复验判据（它们验的是行为，不是像素）。
       ▸ 新建一条 → 点「完成」→ 轻提示右侧点「**撤销**」→ **那条记录应当真的消失**；
         再去 27 回收站看，**不该多出任何东西**
         （撤销 ≠ 移进回收站，这条正是它俩的区别）；接着再记一条、选同一张图，图应正常出现。
       ▸ **编辑**一条已有记录 → 点「保存」→ 这次**不该**出现「撤销」
         （要退回旧内容走 31 → 历史版本，那里恢复一次也是一个新版本）。
       ▸ 新建一条**只加图、一个字不写** → 点「完成」→ 列表里那条是
         **空标题 + 时间 + 「📷 1」**，**这是预期**（不是渲染坏了）；点进去图在。
7. **竖着拍的人像是正的**（编辑页和详情页都要看）。躺倒 = 漏了
   `kCGImageSourceCreateThumbnailWithTransform`。然后**杀掉 App 重进**，图还在
   —— 不在了就说明只把 hash 记进了模型、文件没真写。
8. 35 屏导 JSON → 在「文件」App 里应该同时看到
   `她的信息本-<时间>.json` **和**并排的「…配图」文件夹；
   夹里的 `.jpg` 张数 = 实际引用到的图片张数（不是 0）。
   —— 之前这里注释写着「图片单独放在文件夹里」，而它从没拷过一张图。
9. 33 屏换头像 → 31 屏档案页与 17 屏档案详情**都跟着换**；
   没设头像时显示的是**名字的第一个字**，不是灰色剪影。

**第 10–13 条是这一轮加的（引导 / 轻提示 / 回收站），也都是「不报错但会安静地做错事」：**

10. **删掉 App 重装，第一次打开应该是引导页，不是「小满」。**
    看到「小满」= `seed` 又被自动调用了（它现在只在预览和 `-hi-seed` 下跑）。
    名字留空时主按钮应该是灰的、点不动 —— 空名字会让后面每一屏都显示「（未填）」。
11. 走完引导 → **通知权限弹窗出现在 16 屏讲完理由之后**，不是在 15 屏一进来就弹。
    拒绝掉也应该能进主界面（不能卡在引导里出不去）。
12. 02 屏长按一条记录 → 「删掉」→ 底部弹确认面板 → 「删掉」→
    **顶部出现轻提示且带「撤销」** → 点撤销，记录回来；
    再删一次不撤销 → 去 12 › 回收站，记录在里面、写着「还剩 30 天」。
13. 回收站里恢复一条**原本挂着提醒**的记录 →
    记录回来了，但**提醒不会自己打开**（这是刻意的：`restoreFromTrash` 不碰 `reminder.isOn`）。
    另外确认「提醒会一起取消」那句话是真的 —— 删的时候就调了 `cancel`，
    不是只把开关画成关的样子。

**第 14–15 条是首条记录引导（06 / 21），其中第 14 条就是在验 3.7 节那个 bug：**

14. **新建一条记录，标题分两次慢慢打完**（中间停两三秒，让自动保存跑一次），
    然后退出 → 列表里**应该只有一条**。
    出现两条 = 自动保存每次都在新建（见 3.7 节），这是最容易漏掉的一条回归。
15. 空态（整个 App 一条记录都没有）应该看到三张起手卡 →
    点「她爱吃什么」→ 编辑器里**标题是「她爱吃什么」、分类是喜好、标签有「口味与忌口」，
    但正文是空的、只有一行灰色示范**。
    正文被预填了 = 引导做过头了（那是「替你写」，不是「填一半」）。
    再点「换一题」→ 标题和标签应该跟着换；**如果你自己改过标题，换一题不该覆盖你写的那个**。
    在这屏什么字都不写、直接「保存记录」→ **不该多出一条空记录，也不该弹「已记下」**。

**第 16–17 条是历史版本左右对比（07）。这一屏最容易做错的地方是「恢复」的语义 ——
它必须是「把旧内容落成一个新版本」，不能是「回滚」。第 17 条就是专门验这个：**

16. 31 详情页点「历史版本」**整块** → 进 07 屏。两张对比卡里，
    旧版是**灰底淡字**、当前版是**高亮底 + 主色字 + 描边** ——
    两边不能同一种重量，否则一眼分不出哪边是新的。
    只有差异行该被高亮，两边都有的行保持普通样式（全都标 = 没有重点）。
    只看一版时应该显示「这是第一版 / 再编辑几次，这里就会出现可以左右对比的历史版本」，
    **不是「暂无数据」** —— 这不是错误，是起点。
17. 挑一个旧版本点「恢复」→ 顶部提示应是
    「已回到第 N 版的内容 · **现在是第 M 版**」，其中 **M 必须大于 N**。
    然后回 31 再看 → **版本数应该多了一个，没有少**。
    少了 = 实现成了「删掉后面几版」，页脚那句「当前内容不会丢失」就成了假话。
    再点提示上的「撤销」→ 应该又往回一次，且**版本号继续 +1**
    （这是刻意的：这个 App 的规矩是每次改动都留痕，撤销也不例外）。
    最后一条：恢复一个**带配图**的旧版本 → 配图应该跟着回到那一版的样子；
    被移出的图**不一定**立刻从磁盘消失（那是下次启动 `purgeOrphans` 的事），
    但数据模型里不该再引用它。

**第 18–19 条是搜索与筛选（04 / 30）。这两条验的都是「状态别互相污染」——
静态检查看不出这类问题，只有手点才会暴露：**

18. 进搜索页，打「玫瑰」→ 命中词**在标题和正文里都有浅底色**（不是整句变色）。
    点「只看某一类 › 讨厌的事」→ 列表跟着收窄，且**命中词高亮仍然在**。
    点右下角「筛选」→ 面板底部写「按这些条件，会筛出 N 条」，
    N 必须**等于**你按下去之后统计行里的数字（「按之前说 3 条、按下去出 5 条」就是这一条没做到）。
    点遮罩关掉面板 → **列表不该变**，统计行也不该出现「已筛掉」。
19. 打开筛选面板，把「只看」从『带提醒的』改成『已置顶』→ 那行数字当场变，
    「看 N 条结果」上的 N 也跟着变。按下去 → 面板收起，统计行变成
    「找到 N 条结果 · 已筛掉 M 条」，且 **N + M = 刚才不带筛选时的条数**。
    然后**改一个搜索词**（或点一枚最近搜索、或换一个分类）→
    「已筛掉 M 条」应该**立刻消失**，列表回到未筛状态。
    没消失 = 30 屏那份条件被当成长期设置了（见第 3 节之外的这条约定：
    25 屏的排序是长期的，30 屏的只活一次搜索，两者不能互相写回）。
    最后：点面板里的「重置」→ 三组回到**本月 / 带提醒的 / 最近编辑**
    （是回到出厂，不是全部变空），同时列表上那份筛选一并撤掉。

**第 20 条是键盘输入态（08）。这一屏画的是两件事，而其中只有第二件需要新代码 ——
「输入框加主色描边」`SField` 早就有聚焦态了：**

20. 在 03 新建记录里点内容框 → 键盘弹起，**内容框应该是主色描边 + 一圈很淡的 3px 光晕**
    （不是单纯把线加粗）。再点标题框 → 描边跟着焦点走，
    **同一时刻只该有一个是主色的**。
    键盘上方应该出现一条工具栏，装着三枚胶囊 —— 第一枚写「分类 · 喜好」
    （**warm 底 + 主色字**，不是实心主色：实心会让人以为键盘上还挂着一个
    等着被确认的选项），后两枚是「标签」「提醒 · 开」。
    - 点第一枚 → 出分类菜单；选一个 → 内容区那排分类胶囊跟着变
      （**只有那一排是实心的**，两者不是同一个态）。
    - 点「标签」→ 标签区多一个「新标签N」。它和内容区那颗「＋ 新增标签」
      必须走同一个动作 —— 两处各写一遍的话，将来改命名规则就会只改一处。
    - 点「提醒 · 开」→ 变「提醒 · 关」，**并且这次改动要能落盘**：
      拨完直接杀掉进程再进 → 开关应该还是关着的。
      （这就是补 `onChange(of: remindsMe)` 的原因。拨完接着打字才存、
      拨完直接退就丢，那这个入口就是假的。）
    - **键盘收起时这条工具栏应该一起消失**。没跟着走 = 被实现成了一条钉在屏幕底部
      的视图，而不是 `placement: .keyboard`。
    画布上还有第四枚「记录时间」，**这一版刻意没做** —— 数据模型里只有
    `createdAt` / `updatedAt`（记录什么时候被写下），没有「这件事是什么时候发生的」
    这个字段，03 / 08 两屏的字段区里也都没有它。看到只有三枚不是漏了，见 `README.md` 第三节。

**第 21 条是情绪打标（09 / 10）。它最容易做错的地方是「建议从哪来」——
写死三条示例文案，界面上看着一模一样，但页脚那句承诺就变成假的了：**

21. 从 17 她的档案点「她今天怎么样？」→ 进 09 屏。
    - 顶部那句「打标只记下这一刻，不会读取任何聊天内容」**必须在** ——
      它是这个功能能被接受的前提，不是装饰。
    - 三个档位：**第一次进来时三个都是浅底**；点一个之后 → 那一个变成
      **该档实心 + 白字**（这是「最近一次打的是这档」，不是「你正按着它」），
      同时从底部推上来一块面板。
    - 把三个档位**各点一次**，面板的标题、档位胶囊与内容都要跟着换。
    - **重点是验「建议」**：面板上那三条必须来自你**自己记过的记录**，
      并且带日期与分类。验法是 —— 先去 02 把一条喜好的内容改成一句独一无二的话
      （比如「她讨厌下雨天出门」），再回来打一次标：
      **那三条里应该出现这句话**。不出现 = 建议是写死的，页脚那句承诺是假的。
    - 再验**去重**：「喜好」分类下只有一条记录时，三条建议应该只有**两条**
      （上面那行「从你的档案里找到 N 条」也要跟着变），
      不该出现同一条记录占着两个位置。
    - 档案里一条喜好都没有时 → 应该显示「先去记一条她的喜好或讨厌的事…」，
      **不是「暂无数据」**（那是在描述数据库，不是在说用户下一步该做什么）。
    - 点「2 小时后提醒我」→ **去 05 屏应该真的多出一条提醒**（今天 +2 小时、仅一次）。
      只跳转、不建提醒 = 这个按钮是句空话。
    - 点遮罩或右上角 × → 关面板，列表不该变。
    最后一条：**四行「系统级入口」现在全部通了** ——
    「在通知上直接换档位」走的是 36 屏那套通知内容扩展；
    「长按 App 图标」「快捷指令」上一轮接上；这一轮补完了**分享扩展**（第 22 条）。
    - 顺手验一下长按图标：**先杀掉 App**，再长按图标 → 菜单里应该有三项。
      选一项 → 打开 App → 09 屏那行「最近一次打标」应该变成刚选的那一档。
      **如果菜单出来了、点了却什么都没发生**，八成是 type 前缀（`hi.mood.`）
      或图标名与代码里对不上 —— 系统对这种不一致**不报任何错**。
      还有一处更隐蔽的：冷启动时那一项是从 `launchOptions` 里取的，
      走 `didFinishLaunching` 而不是 `performActionFor`；
      **只接后者的话，「杀进程之后长按图标打标」会静默失效** ——
      而那恰恰是最常用的路径（谁会一直挂着 App）。

**第 22 条是分享扩展。它有一半的失败方式发生在 App 外面 —— 看不到任何界面提示，
所以这一条更该照着走：**

22. 先去「备忘录」写**三行**话（好验标题取自首行）→ 分享 → 在分享面板里找
    **「存进她的信息本」**。
    - **面板里根本没有这一项** = 三个不同的原因，都不报错：
      `NSExtensionPointIdentifier` 不是 `com.apple.share-services`、
      `NSExtensionActivationRule` 少了外层那一层、或主 App 的 `dependencies` 里
      漏了 `embed: true`。（这三条 `check-project.py` / `check-swift.js` 都查。）
    - 打开面板：正文应该是**原文、一个字都没改**，下面写着「原文 · N 字」；
      底部按钮上写着**「先选一个分类」**，而不是一个灰着的「保存」。
    - **不选分类点按钮 → 应该点不动**（分类是用户给的判断，不能替他猜）。
      选一个 → 按钮变成「存下这条」并变主色。
    - 点「存下这条」→ 面板关掉 → 回到 App 顶部应该滑下
      **「已存下分享的 1 条」**（条数是数出来的，不是写死的）。
      没有提示 = 收件箱没落库。查 App Group：主 App 与分享扩展必须是**同一个**
      `group.com.herinfo.app` —— 只有一边开了，`UserDefaults(suiteName:)` 返回 nil，
      表现正是**静默丢失**。
    - 去 02 列表：标题是**第一行**（超 20 字带省略号）、正文是整段、分类是你选的那个。
    - **杀进程测试（这一条最该验）**：分享完**先杀掉 App**再打开 —— 记录仍然在。
      不在了 = 落库那一步挂在了「扩展里自己写库」上。那是错的：
      两个进程同时开一个 SwiftData 库会写冲突，所以扩展只能往收件箱里丢。
    - 再从 Safari 分享一个网页 → 正文是那个**完整网址**，而**标题是域名**
      （`https://www.xiaohongshu.com/discovery/item/66f…` 在列表里根本认不出来，
      域名一眼就知道是哪儿的。正文一个字没少）。
      （扩展只声明了文本与网址两样，所以图片、文件的分享面板里**刻意不会出现**它 ——
      在那些地方冒出来、点进去却什么都存不了，比不出现更让人困惑。）
    - 「没读到文字」那个空态**不好人为触发**（只在来源声明给了文字、实际读不出内容时
      才走到），所以它靠代码守着：整理后正文为空时 `Compose.item` 返回 nil，
      **不会存下一条空记录**。要核对就看面板里那句话在不在：
      「这次分享里没有读到文字」+ 一句「图片先存相册，再回 App 里加进那条记录」。

