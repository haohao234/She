# ============================================================
#  行尾统一
# ============================================================
#  Windows 上的 Git 默认 core.autocrlf=true，会把工作区文件换成 CRLF。
#  这在这里是个真问题：.github/workflows/ios-build.yml 里的 shell 脚本
#  一旦在仓库里带上 CRLF，到 macOS runner 上执行就会报
#  「/bin/sh: 1: Syntax error」之类的错 —— 而且报错信息完全指不到行尾。
#
#  所以：仓库里一律存 LF，工作区也一律 checkout 成 LF。
# ============================================================

* text=auto eol=lf

# 二进制文件不参与行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
*.zip binary
