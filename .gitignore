# 由 XcodeGen 生成，不入库 —— project.yml 才是真相。
# 想改工程结构，改 project.yml 再跑 `xcodegen generate`。
*.xcodeproj
*.xcworkspace

# 构建产物
build/
DerivedData/
*.ipa
*.dSYM.zip

# macOS
.DS_Store

# 推送脚本（push-to-github.bat）从 v4 起**入库**，不再忽略：
# 它自己按「便携版 → 正式版 → PATH」三级找 git，不含任何本机路径，
# 换台机器也能用。改它之前先读文件头的「编码铁律」——
# 它必须是 GBK(936) 编码 + CRLF 行尾，UTF-8 会被 cmd 从行中间截断。

# 推送脚本每次运行都会留一份现场记录（含本机路径与 git 原始输出），
# 是拿来看的，不是拿来入库的。
推送日志.txt

# 工具在本机跑诊断 / 拉 CI 日志时留下的工作目录。
# 它属于「这台机器的工作台」，不属于这个工程。
.workbuddy/
