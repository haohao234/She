# 由 XcodeGen 生成，不入库 —— project.yml 才是真相。
# 想改工程结构，改 project.yml 再跑 `xcodegen generate`。
*.xcodeproj
*.xcworkspace

# 构建产物
build/
DerivedData/
*.ipa
*.dSYM.zip

# macOS
.DS_Store

# 本机专用：双击就能推送的辅助脚本。
# 它硬编码了本机的便携版 git 位置，对别人没意义，所以不进仓库。
push-to-github.bat

# 推送脚本每次运行都会留一份现场记录（含本机路径与 git 原始输出），
# 是拿来看的，不是拿来入库的。
推送日志.txt
