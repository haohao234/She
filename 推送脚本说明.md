# 推送脚本说明（v5）

> 面向：要用「双击一下就把代码送到 GitHub」的人（也就是未来的我自己）。
> 文件：`push-to-github.bat`

---

## 一、这次「还是失败」，失败的不是网络

现象是：双击脚本，跑完看到「失败 / 已取消」，于是以为代码没推上去。
真凶有三个 —— **一个在凭据链、一个在判断逻辑、一个在文件编码，都不在网络。**

### 真凶 1：凭据助手选择器每次认证空跑 40 秒（最致命）

系统级 gitconfig 里配了这么一条：

```
credential.helper=helper-selector
```

这个「凭据助手选择器」每次认证要**空跑约 40 秒**（它挨个探测环境里有哪些可用的凭据助手）。
而 `git push` 一次要认证两回，80 秒就这么没了。抓到的真实日志：

```
13:29:25  => Send header: GET .../info/refs?service=git-receive-pack
13:29:25  <= Recv header: HTTP/1.1 401 Unauthorized        ← 需要凭据
13:29:26  run_command: 'git credential-helper-selector get'
13:30:12  ...                                              ← 46 秒过去了
13:30:12  git config --global credential.helperselector.selected manager
13:30:13  <= Recv header: HTTP/1.1 200 OK                   ← 认证终于过了
13:30:13  run_command: 'git credential-helper-selector store'
13:30:54  ...                                              ← 又 41 秒
```

**它的杀伤力在于「什么都不打印」**：命令在超时线上被掐死，屏幕上没有任何错误信息，
看起来就是「网络不通」—— 于是人被赶到换网络、查防火墙的方向去。

而 `git ls-remote` 是**匿名读取**（公开仓库不需要凭据），所以它秒过。
这正好解释了「能读、不能写」这个极其反直觉的现象。

#### 一个最容易误判的地方：`credential.helper` 是【累加】的，不是覆盖

排查时看到 `~/.gitconfig` 里明明已经指向 GCM，很容易就此以为「凭据没问题」：

```
[credential]
	helper = !"C:/…/git-credential-manager.exe"
```

**但 git 不会用后面那条覆盖前面那条 —— 它把每一层、每一条都追加进同一个列表**。
实际生效的链条是：

| 层级 | 值 | 结果 |
| --- | --- | --- |
| system（便携版 git 的 `etc/gitconfig`） | `helper-selector` | ← 40 秒白等在这 |
| global（`~/.gitconfig`） | `!…/git-credential-manager.exe` | 它也要再跑一遍 |

两个都会被调用。所以「global 已经配对了」并不能免除那 40 秒 ——
第一版修法只把它从**这个仓库**摘掉，其他仓库照旧。

#### 修法：在优先级最高的那层写一条空值，把前面累积的全部清掉

git 规定 `credential.helper` **出现空值时会清空前面积累的列表**。
所以先写空值、再写要用的助手，就能把 system 级那条整条摘掉：

```bash
git config --global --replace-all credential.helper ""
git config --global --add credential.helper manager
```

两处细节值得说明：

- **用短名 `manager`，不写绝对路径**。它会靠 PATH 解析到 `git-credential-manager`。
  写死绝对路径（`…/PortableGit/versions/1.2.0/…`）在 WorkBuddy 升级便携版 git
  之后会直接失效，而且失效得比 40 秒更难看 —— 那会变成「认证失败」。
- **没有用 `--system --unset`**。system 级文件在便携版 git 的安装目录里，
  WorkBuddy 更新时会连二进制一起换掉，改动会被冲掉；而 global 里的空值重置是
  **运行时的、可逆的** —— 删掉 `~/.gitconfig` 里那两行就完全复原，也不碰安装目录。

实测对照：**同一次 `git credential fill` 从「80 秒+超时」变成 1 秒**，
选择器调用次数 `2 → 0`。

同一份逻辑也固化进脚本的 `:tune_creds`，仓库级再加一道保险 ——
换台机器 clone 下来照样自愈，不必依赖 global 有没有被别的东西改坏：

```bash
git config --local --replace-all credential.helper ""
git config --local --add credential.helper <取全局第一个非空值，取不到就用 manager>
```

> `:tune_creds` 这一步在本轮还顺手修掉一个潜伏 bug：它原来用 `--get` 只取全局的
> **第一条**，再用 `findstr "credential-manager"` 过滤。全局加了空值重置之后，
> 第一条就是空的 → 整步静默跳过；而如果全局写的是短名 `manager`，
> `findstr` 同样匹配不上。现在改成 `--get-all` 取第一个**非空**值，
> 取不到就退回 `manager`。

### 真凶 2：把「远程非空」当成了「必须覆盖」

v3 在第 6 步做的是：

```bat
if "!RCOUNT!"=="0" goto :push_normal      REM 远程是空的 → 直接推
...                                        REM 否则一律当成「两条无关历史，要不要覆盖？」
```

可是**远程只要推成功过一次就永远非空**。于是第二次以后每次跑脚本，它都拿
「用户自己上次推上去的那批提交」去问：

```
! 远程不是空的。本地和远程是两条互不相关的历史，直接推会被拒绝。
  上面这些内容会被本地版本覆盖掉。
  覆盖并推送？(Y/N)
```

不敢按 Y，按 N 退出 —— 屏幕上最后一行就是「已取消」。
**看起来是推送失败，实际是脚本问错了问题。**

正确的判断不是「远程空不空」，而是「本地能不能快进」：

| 本地与远程的关系 | 该做什么 | v3 的行为 | v4 的行为 |
| --- | --- | --- | --- |
| 远程是本地历史的祖先 | 快进推送，零损失 | 问「要不要覆盖」 | 直接推 ✅ |
| 两边 SHA 一样 | 什么都不用做 | 问「要不要覆盖」 | 报「已经一致」✅ |
| 各有对方没有的提交 | 这才该问覆盖 | 问「要不要覆盖」 | 问「要不要覆盖」✅ |

### 真凶 3：`.bat` 存成 UTF-8，被 cmd 从行中间截断

这是更隐蔽的一个。cmd **解析 `.bat` 文件用的是系统 ANSI 代码页（中文机器 = 936）**，
`chcp 65001` 只改控制台**输出**，改不了它**读文件**的方式。
文件是 UTF-8 时，中文会在行中间被切断，症状是屏幕冒出一堆：

```
'��过、还是网络不通，' is not recognized as an internal or external command
'是这种，版本号通配，不写死）' is not recognized as an internal or external command
```

更糟的是 `set` / `goto` 也可能一起被切断。实测抓到的一次：

```bat
set "RESULT_TXT=推送成功（已核对远程与本地一致）"    ← 被切成两条命令
```

结果最后的结论行打印成空：

```
  OK                      ← 本该是「OK 推送成功（已核对远程与本地一致）」
```

**修法：文件改为 GBK(936) 编码 + CRLF 行尾，代码页改成 `chcp 936`。**
这条规矩写在文件头的「编码铁律」注释里，改脚本前先读它。

> 改这个文件时不要用默认按 UTF-8 保存的编辑器。若必须用，改完转回 GBK。

---

## 二、v4 还顺手改了两处

### 收尾不再相信 `git push` 的退出码

退出码会骗人，实测过三种形态：

1. `git push 2>&1 | tail -3` 之后，`$?` 是 `tail` 的退出码，**永远是 0**；
2. 网络半途断掉时，git 可能先打印一段像成功的内容再报错；
3. **最极端的一种：`git push` 退出码 `0`、输出完全为空**，`&&` 后面那句「成功」照常执行 ——
   但远程 ref 一个字没动（2026-09-11 实测，那条命令是 `timeout 60 git push … > log 2>&1`，
   事后 `log` 是空的）。**「没报错」和「退出码是 0」都不等于成功。**

所以 v4 在最后加了一步**客观核对** —— 拿远程的真实 SHA 和本地 `HEAD` 逐字比对：

```
  [核对] 确认 GitHub 上到底是哪一条提交 …
        本地 HEAD  1448cc5
        远程 HEAD  1448cc5

        OK  已核对：GitHub 上的内容和你本地完全一致。
```

只有这两个值相同，才会打印「推送成功」。

### `fetch` 改用完整 refspec

`git fetch origin main` 只写 `FETCH_HEAD`，**不更新 `refs/remotes/origin/main`**，
于是 `git status` 会一直显示：

```
## main...origin/main [gone]
```

看着像「远程分支丢了」，其实只是本地从来没把远程分支记下来。
改成 `git fetch origin "+refs/heads/main:refs/remotes/origin/main"` 之后就正常了。

---

## 三、自测：三个隔离场景

在临时目录里用本地 bare 仓库当「远程」跑回归，**不碰真实仓库、不碰全局 git 配置**：

| 场景 | 期望 | 实测 |
| --- | --- | --- |
| 本地与远程已一致 | 报「已经一致」并核对通过 | ✅ `OK 推送成功（已核对远程与本地一致）` |
| 本地领先 1 个提交 | 走快进，**不问覆盖** | ✅ 「可以快进：远程那 1 个提交本地都有」，推送后核对一致 |
| 真分叉 | 才问覆盖；按 N 不破坏远程 | ✅ 给出 `git pull --rebase` 的合并建议，远程 SHA 未变 |
| 凭据链 | 不再触发那个 40 秒的选择器 | ✅ 选择器调用 `2 → 0`；`git credential fill` 由 `80s+` 超时 → **1s** |
| 全局修复（在**非仓库目录**实测，只读 system + global） | 其他项目也不再白等 | ✅ 选择器调用 `0` 次、GCM `3` 次，耗时 **1s** |

另外：改编码前后各跑一遍，`is not recognized` 这种乱码行
**从几十条降到 0 条**。

---

## 四、第四种失败：`github.com` 这一个域名整段不通（2026-09-11 下午实测）

前三种都排除掉之后还剩一种：**不是断网、不是凭据、不是抖动，而是 `github.com:443`
这一个域名 TCP 完全握不上手**。它和第一节那三种的表现很像（都是「连不上」），
但处置方式完全相反 —— 前三种要改配置，这种**改什么都没用**。

### 怎么把它和前三种区分开

分域名探测一次就清楚了（同一分钟、同一台机器）：

| 域名 | 结果 | 说明 |
|---|---|---|
| `api.github.com` | **http 200 · connect 0.07s** | 通，秒回 |
| `codeload.github.com` | **http 301 · 0.09s** | 通 |
| **`github.com`** | **http 000 · connect 0.000s · 15s 全超时** | **TCP 根本没握上手** |

DNS 正常（`github.com` → `20.205.243.166`）、token 有效（`permissions` 里 `push: true`）、
`ls-remote` 匿名读也可能通 —— **所以「整个 GitHub 不通」和「整机断网」这两个判断都是错的**。
只要 `connect` 那一列是 `0.000s` 而不是「连上了但握手慢」，就是这条路堵死，不是抖动。

```bash
curl -sS --max-time 15 -o /dev/null \
  -w "http=%{http_code} connect=%{time_connect}\n" https://api.github.com
curl -sS --max-time 15 -o /dev/null \
  -w "http=%{http_code} connect=%{time_connect}\n" https://github.com
```

### 绕过去：走 Git Data API 重建这几个提交

`git push` 只会走 `github.com`，那条路堵死它就彻底没戏；但 **API 走的是 `api.github.com`**，
那条路是通的。所以有 **`push_via_api.py`**（跨项目通用，已沉淀在推送验证技能里：
`C:\Users\haohao\.workbuddy\skills\github-push-verify-ci\scripts\push_via_api.py`）——
用 Git Data API 把本地还没推上去的提交**原样变成远端提交**：

```
blob（逐文件）→ tree（带 base_tree）→ commit（带 parent、author/committer 原样）
→ PATCH /git/refs/heads/main（force: false）
```

**为什么这不是伪造历史**：GitHub 的 blob / tree / commit 都是**标准 git 对象、内容寻址** ——
只要文件内容、mode、parent、author/committer 与时间戳完全一致，**算出来的 SHA 就与本地一模一样**。
脚本每条提交都比对一次 SHA，**不一致就不更新远端分支**（只留几个游离对象，无害）。
实测 4 条提交逐个 `✓ sha 一致`。

三条纪律（都踩过）：

1. **用 `curl` 发请求，不用 Python 的 `urllib`** —— Windows 自带的 `curl` 走 **Schannel**（通），
   Python 走 **OpenSSL**（一律 `SSL: UNEXPECTED_EOF_WHILE_READING`）。**这是能不能通的问题，不是风格问题**；
2. **body 走 stdin（`--data-binary @-`）** —— Windows 命令行有长度上限，65 KB 文件的 base64 会顶穿；
3. **网络错重试、业务错不重试** —— curl 退出码 7/28/35/52/55/56 重试，
   22（HTTP ≥ 400）不重试；配 `--fail-with-body` 才拿得到错误体。
   另外 **`rc=23` 是「客户端写失败」不是网络失败**（`-w %{http_code}` 照常输出 200）。

还有个容易踩的：**`git diff-tree` 必须带 `-c core.quotePath=false`** ——
否则中文文件名会被转义成 `"\344\270\255"` 那种八进制串，路径直接对不上。

> **什么时候用它**：`push-to-github.bat` 那 3 次重试全失败、
> 而且上面那张表里 `github.com` 的 `connect` 是 `0.000s` 的时候。
> 只要 `push-to-github.bat` 能通，就用不着它 —— 多一层不如少一层。
>
> 用法：`python <技能目录>/github-push-verify-ci/scripts/push_via_api.py <仓库目录> <owner/repo> [分支]`
> 例：`python …/push_via_api.py D:/x/她的信息本-iOS haohao234/She main`
> token 由脚本自己从 `git credential fill` 取（与 `git push` 用的是同一个），只在内存里，不落盘、不打印。

---

## 五、以后怎么确认「到底成没成」

三件事，任选其一，全都比盯着窗口猜可靠：

1. **看脚本最后那段 `[核对]`** —— 「远程 HEAD」和「本地 HEAD」一样就是成了；
2. **自己在命令行核对**（最权威）：

   ```bash
   git ls-remote origin refs/heads/main
   git rev-parse HEAD
   ```

   两个 SHA 逐字相同 = 代码确实在 GitHub 上。

3. **打开仓库网页看一眼**：`https://github.com/haohao234/She`
   代码在不在，一眼就看得出来。

至于编译，看 Actions 就够了 —— 但要分清**绿勾回答的是什么**：
**绿 = 编译过了，而且 0 warning**（后者是流水线里一条会红的检查步骤，见下）；
**红 = 要么编译错，要么有 warning**，两种都把清单发出来。
历史上那 3 次红叉都在 `87621f5` 之前；**从 `d188b67` 起没有一次回退** ——
`d188b67 → 130540e → 801844a → 31dbafd → ce92c47 → 191b14f → 15b3ba0 → f2e44cd → b4d5865 → 0aee909
→ 7040200 → da0e65d → 20c2469 → 45fb77a → 789d0e5 → 4d3465a → 79cb7dd → 7672c95 → 011af10
→ c362e13 → 1cfe016 → 2aacaef`
连续 **22 次成功**。

（`d188b67` 就是第 4 轮，所以这个链的长度就是连续成功的次数 —— **按 CI 实际运行次数计**。
一次推送里带多个提交时只在最后一个提交上跑一次，所以 run 的数量会比提交数少几个，
这不是漏跑；而「把结论写回文档」这一提交自己也会跑一次，所以文档里的数字天然滞后一次。）

> **「成功」不等于「干净」—— 这句话是踩出来的。** 第 12~25 轮（连续 14 轮）每一轮都带着同一条
> warning（07 屏那个文件里一个没用到的变量绑定），而文档一直写着「0 error / 0 warning」。
> 原因很朴素：**只看了绿勾，没看流水线自己数出来的条数。**
> 现在流水线多了一条「把 warning 也当失败」的步骤 —— 从此那句话是**断言**。
> 代价是 runner 的 Xcode 升级带来新 deprecation 警告时 CI 也会红；
> 那种红是提醒不是回归，看清单就知道是什么。**唯独不能让它在日志里静静躺着。**

> 上面那张表里「全局修复」那一行之所以要在**非仓库目录**测，是因为那个目录里
> 不存在仓库级配置，`git` 只会读 system + global 两层 ——
> 这样测出来的结果才能代表「你其他项目现在是什么状态」。

---

## 六、一句话总结

- **「能读取」不等于「能写入」**：`ls-remote` 匿名读取秒过，`push` 要认证 —— 而认证链上
  那个 40 秒的选择器会让 push 静默超时。遇到「读得到、推不上」，先查 `credential.helper`；
- **`credential.helper` 是累加的，不是覆盖的**：global 配对了不代表没坑，
  system 里那条一样会跑。要屏蔽前面累积的，就在最外层写一条**空值**；
- 「远程非空」不等于「要覆盖」—— 日常场景是**快进**，不该问；
- `.bat` 必须是 **GBK + CRLF**，否则 cmd 会把中文行和 `set` 语句一起切碎；
- **「连不上」要分域名判断**：`connect=0.000s` 是这条路堵死（改配置没用），
  「连上了但握手慢」才是抖动（重试有用）—— 两者的处置方向完全相反；
- `github.com` 堵死时还有一条路：**走 `api.github.com` 的 Git Data API 重建提交**
  （内容寻址 ⇒ 重建出来的 SHA 与本地一致，不是伪造历史）；
- 「推没推上去」只认 **远程 SHA == 本地 HEAD**，不认退出码、不认窗口里那句口头成功。
