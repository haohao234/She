# 推送脚本说明（v4）

> 面向：要用「双击一下就把代码送到 GitHub」的人（也就是未来的我自己）。
> 文件：`push-to-github.bat`

---

## 一、这次「还是失败」，失败的不是网络

现象是：双击脚本，跑完看到「失败 / 已取消」，于是以为代码没推上去。
真凶有两个，**一个和网络无关，一个是编码**。

### 真凶 1：把「远程非空」当成了「必须覆盖」

v3 在第 6 步做的是：

```bat
if "!RCOUNT!"=="0" goto :push_normal      REM 远程是空的 → 直接推
...                                        REM 否则一律当成「两条无关历史，要不要覆盖？」
```

可是**远程只要推成功过一次就永远非空**。于是第二次以后每次跑脚本，它都拿
「用户自己上次推上去的那批提交」去问：

```
! 远程不是空的。本地和远程是两条互不相关的历史，直接推会被拒绝。
  上面这些内容会被本地版本覆盖掉。
  覆盖并推送？(Y/N)
```

不敢按 Y，按 N 退出 —— 屏幕上最后一行就是「已取消」。
**看起来是推送失败，实际是脚本问错了问题。**

正确的判断不是「远程空不空」，而是「本地能不能快进」：

| 本地与远程的关系 | 该做什么 | v3 的行为 | v4 的行为 |
| --- | --- | --- | --- |
| 远程是本地历史的祖先 | 快进推送，零损失 | 问「要不要覆盖」 | 直接推 ✅ |
| 两边 SHA 一样 | 什么都不用做 | 问「要不要覆盖」 | 报「已经一致」✅ |
| 各有对方没有的提交 | 这才该问覆盖 | 问「要不要覆盖」 | 问「要不要覆盖」✅ |

### 真凶 2：`.bat` 存成 UTF-8，被 cmd 从行中间截断

这是更隐蔽的一个。cmd **解析 `.bat` 文件用的是系统 ANSI 代码页（中文机器 = 936）**，
`chcp 65001` 只改控制台**输出**，改不了它**读文件**的方式。
文件是 UTF-8 时，中文会在行中间被切断，症状是屏幕冒出一堆：

```
'��过、还是网络不通，' is not recognized as an internal or external command
'是这种，版本号通配，不写死）' is not recognized as an internal or external command
```

更糟的是 `set` / `goto` 也可能一起被切断。实测抓到的一次：

```bat
set "RESULT_TXT=推送成功（已核对远程与本地一致）"    ← 被切成两条命令
```

结果最后的结论行打印成空：

```
  OK                      ← 本该是「OK 推送成功（已核对远程与本地一致）」
```

**修法：文件改为 GBK(936) 编码 + CRLF 行尾，代码页改成 `chcp 936`。**
这条规矩写在文件头的「编码铁律」注释里，改脚本前先读它。

> 改这个文件时不要用默认按 UTF-8 保存的编辑器。若必须用，改完转回 GBK。

---

## 二、v4 还顺手改了两处

### 收尾不再相信 `git push` 的退出码

退出码会骗人，实测过两种形态：

1. `git push 2>&1 | tail -3` 之后，`$?` 是 `tail` 的退出码，**永远是 0**；
2. 网络半途断掉时，git 可能先打印一段像成功的内容再报错。

所以 v4 在最后加了一步**客观核对** —— 拿远程的真实 SHA 和本地 `HEAD` 逐字比对：

```
  [核对] 确认 GitHub 上到底是哪一条提交 …
        本地 HEAD  1448cc5
        远程 HEAD  1448cc5

        OK  已核对：GitHub 上的内容和你本地完全一致。
```

只有这两个值相同，才会打印「推送成功」。

### `fetch` 改用完整 refspec

`git fetch origin main` 只写 `FETCH_HEAD`，**不更新 `refs/remotes/origin/main`**，
于是 `git status` 会一直显示：

```
## main...origin/main [gone]
```

看着像「远程分支丢了」，其实只是本地从来没把远程分支记下来。
改成 `git fetch origin "+refs/heads/main:refs/remotes/origin/main"` 之后就正常了。

---

## 三、自测：三个隔离场景

在临时目录里用本地 bare 仓库当「远程」跑回归，**不碰真实仓库、不碰全局 git 配置**：

| 场景 | 期望 | 实测 |
| --- | --- | --- |
| 本地与远程已一致 | 报「已经一致」并核对通过 | ✅ `OK 推送成功（已核对远程与本地一致）` |
| 本地领先 1 个提交 | 走快进，**不问覆盖** | ✅ 「可以快进：远程那 1 个提交本地都有」，推送后核对一致 |
| 真分叉 | 才问覆盖；按 N 不破坏远程 | ✅ 给出 `git pull --rebase` 的合并建议，远程 SHA 未变 |

另外：改编码前后各跑一遍，`is not recognized` 这种乱码行
**从几十条降到 0 条**。

---

## 四、以后怎么确认「到底成没成」

三件事，任选其一，全都比盯着窗口猜可靠：

1. **看脚本最后那段 `[核对]`** —— 「远程 HEAD」和「本地 HEAD」一样就是成了；
2. **自己在命令行核对**（最权威）：

   ```bash
   git ls-remote origin refs/heads/main
   git rev-parse HEAD
   ```

   两个 SHA 逐字相同 = 代码确实在 GitHub 上。

3. **打开仓库网页看一眼**：`https://github.com/haohao234/She`
   代码在不在，一眼就看得出来。

至于编译，看 Actions 就够了：**绿色勾 = 通过，只有红叉才需要把错误发出来**。
历史上那 3 次红叉都在 `87621f5` 之前；从 `d188b67` 起连续通过。

---

## 五、一句话总结

- 「远程非空」不等于「要覆盖」—— 日常场景是**快进**，不该问；
- `.bat` 必须是 **GBK + CRLF**，否则 cmd 会把中文行和 `set` 语句一起切碎；
- 「推没推上去」只认 **远程 SHA == 本地 HEAD**，不认退出码、不认窗口里那句口头成功。
